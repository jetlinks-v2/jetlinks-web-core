# 业务应用切换与菜单上下文

## 当前结论与目标

项目与应用运行态前端左上角标题作为业务应用切换入口。可选项来自 `GET /business-application/me`；进入业务应用时，应用运行态的 project storage 会写入 `scope: <applicationId>`，`package.ts` 请求拦截器据此为该应用下所有接口携带 `X-Application-Scope: <applicationId>`。项目本身不写入 `scope`，因此项目接口不携带应用 Scope。租户端启动或刷新时不初始化业务应用上下文，也不为系统配置自动请求业务应用列表或详情；租户应用管理页的显式 CRUD 不在此限制内。

应用选择保存在当前浏览器标签页的 `sessionStorage` 中，应用运行态的接口 Scope 保存在对应 `project_<applicationId>` 的 project storage 中。运行边界统一通过 `src/utils/business-application-runtime.ts` 判断：仅非微应用下的项目路径及其应用入口初始化应用列表；租户根端即使 `VITE_APP_ENVIRONMENT='saas'`，启动时也不会自动请求 `/business-application/me` 或 `/business-application/{id}`，菜单初始化还会显式传入 `false`，避免读取当前标签页遗留的应用 Scope。独立项目端即使 `VITE_APP_ENVIRONMENT=''` 仍会请求 `/business-application/me`。项目运行态在没有显式 `applicationScope` 时默认保留项目菜单；子账号登录成功后会主动查询应用列表，并通过同一套“进入应用”逻辑进入第一个业务应用。若当前账号没有业务应用，则继续走原项目入口逻辑。

应用端 Header 右侧的应用快捷入口同样消费 `src/store/businessApplication.ts`，展开弹层时通过 `refreshApplications()` 重拉 `/business-application/me`。租户端不会挂接这个请求入口。刷新会保留仍然有效的当前应用 Scope；当前应用已删除时才回退到项目入口或首个可用应用，避免列表同步意外改变菜单上下文。

当业务应用列表非空时，下拉中会附加一个项目入口。该入口使用项目菜单请求，不写入 storage scope，用于从业务应用切回项目本身。

## 影响范围与 owning module

owning module：`ui/jetlinks-web-core`；子账号登录入口位于 `ui/modules/saas-manager-ui`。

- `src/api/application.ts`：业务应用类型和 `/business-application/me` 请求。
- `src/store/businessApplication.ts`：应用列表、当前应用、初始化和切换编排。
- `src/utils/business-application-runtime.ts`：统一判断项目路径及其应用入口是否启用应用切换，并排除租户端与微应用。
- `src/utils/application-scope.ts`：标签页级 Scope 读取、选择和持久化。
- `src/utils/application-access.ts`：生成应用入口 URL；project storage 关闭的独立项目端仅支持同源切换，不写入 `project_<applicationId>`。
- `src/utils/project-storage.ts`：保留 `projectName`，避免切入业务应用后丢失项目下拉名称。
- `src/package.ts`：请求拦截器从当前 project storage 读取 `scope`，存在时为 axios 与 NDJSON 请求补充 `X-Application-Scope`。
- `src/store/menu.ts`、`src/store/menuRuntime.ts`：按应用重载菜单，替换旧动态路由和按钮权限，返回首菜单。
- `src/router/startup.ts`、`src/router/index.ts`：项目/应用登录会话先初始化应用，无可进入应用时进入 403；租户会话跳过应用初始化。
- `src/layout/components/BusinessApplicationSwitcher.vue`、`BusinessApplicationIcon.vue`、`src/layout/BasicLayoutPage.vue`：复用现有标题位和 Ant Design Vue Dropdown/Menu 实现切换入口。
- `../modules/saas-manager-ui/views/login/components/SubAccountLogin.vue`：子账号登录后先保存项目访问上下文，再进入首个业务应用；没有业务应用时回到原项目跳转。

## 实施边界

- 不修改 `runtime-ui/`。
- 不修改后端接口契约。
- 不为项目本身写入 `scope`，只让业务应用运行态携带 `X-Application-Scope`。
- 菜单请求失败时保留原应用、原菜单、权限和当前路由。
- 不改业务页面壳层、菜单模型或普通业务接口请求上下文。

## 租户、项目与应用布局分层

目标是在不改变菜单、路由、权限和业务页面结构的前提下，将租户端、项目端、应用端的布局样式收口到三个独立壳层。`src/layout/BasicLayoutPage.vue` 只负责根据当前作用域选择壳层；菜单状态、导航、响应式尺寸、二级菜单和布局级生命周期统一由共享 composable 编排，避免三端复制相同处理逻辑。

布局入口优先读取当前作用域系统配置的 `front.layoutVariant`，允许值为 `tenant`、`project`、`application`。配置缺失或非法时按运行时上下文回退：项目 Scope 或项目运行态使用 `project`，业务应用 Scope 使用 `application`，其余默认使用 `application`；租户端需要租户壳层时应显式配置 `tenant`。进入应用端后，`ApplicationLayoutPage.vue` 再从当前应用详情的 `configuration.layoutVariant` 设置共享壳层；历史应用缺失或值非法时回退 `application`。现有 `front.layout` 继续只表示 `side`、`mix`、`top`，不承担壳层类型语义。

实施入口：

- `src/layout/runtime/layoutVariant.ts`：布局类型规范化与纯函数解析。
- `src/layout/hooks/useBasicLayoutVariant.ts`：读取作用域系统配置和运行时上下文。
- `src/layout/hooks/useBasicLayoutController.ts`：共享布局状态、菜单和生命周期。
- `src/layout/shells/`：租户、项目、应用三个独立壳层入口及当前共享渲染骨架。
- `src/layout/BasicLayoutPage.vue`：布局分发和共享上下文提供者。

本次只建立分层和样式作用域，三个壳层默认复用当前布局骨架，避免在没有视觉方案时改变用户界面。后续样式差异只在对应壳层作用域内演进，不向业务页面或其他运行态泄漏。

## 应用模板布局变体前端接入计划

目标：前端新增业务应用模板时统一写入扩展字段 `layoutVariant: "application"`；基于模板新增业务应用时，将模板的布局变体复制到应用的 `configuration.layoutVariant`；进入应用端后，`ApplicationLayoutPage.vue` 只读取当前应用详情中的配置并传给共享布局壳层。

影响范围与 owning module：模板新增入口只处理 `ui/modules/authentication-manager-ui/views/application-center/Template/`；业务应用创建和模板数据转换位于同模块的 `views/application-center/ProjectApplication/`；应用详情类型与布局消费位于 `ui/jetlinks-web-core/src/api/application.ts` 和 `src/layout/shells/ApplicationLayoutPage.vue`。不修改 `ui/modules/saas-manager-ui`、`runtime-ui` 或后端。

不做：不新增模板表单项，不修改模板或业务应用后端实体，不让 `ApplicationLayoutPage.vue` 请求模板详情，不调整菜单、权限和系统配置接口，不改变租户端与项目端壳层。现有模板或应用缺少该字段、字段值非法时继续使用 `application`。

实施步骤：先补齐应用管理模块中的模板、应用运行配置和前端展示模型布局字段类型；再让应用管理模块的模板新增入口提交默认扩展字段；随后在业务应用创建编排中从已选模板复制合法的布局变体到 `configuration.layoutVariant`；最后由 `ApplicationLayoutPage.vue` 从业务应用 Store 已加载的当前应用详情中同步派生布局变体。

风险与待确认：该方案只影响通过当前前端创建的新模板和新应用；历史模板、历史应用以及绕过前端直接调用接口创建的数据不会自动补齐字段。应用更新时必须保留已有 `configuration.layoutVariant`，避免编辑名称、语言、时区或域名后覆盖布局配置。

验证方式：本次按用户要求不执行构建、类型检查、测试或页面冒烟；交付时列出待确认的模板创建请求、应用创建请求与应用端布局消费链路，不把未执行项记为已验证。

实现结果：应用管理模块新增模板时写入 `layoutVariant: "application"`；模板列表归一化后保留合法布局值，并在新增业务应用时复制到 `configuration.layoutVariant`。应用更新沿用原有配置展开逻辑保留该快照；`ApplicationLayoutPage.vue` 直接消费业务应用 Store 中的当前应用详情，不发起模板查询。

验证结果：按用户要求未执行构建、类型检查、测试、静态检查或登录态页面冒烟；模板创建请求、应用创建请求和应用端布局切换仍待目标环境确认。

## 项目设置导航与 Header 调整计划

项目端采用“当前项目详情工作区”：普通业务二级菜单保持左侧导航，设置域使用顶部横向 Tabs；项目 Header 右侧复用现有业务应用 Store 提供应用切换入口，并增加权限感知的设置入口。项目端不提供菜单收起按钮，租户端和应用端保持原行为。

设置菜单由 `baseMenu.json` 声明 `options.routeTarget`，不在布局层维护来源菜单 code。项目端直接使用隐藏的 `system` 父菜单作为目标；菜单权限过滤完成后、动态路由生成前，通用菜单装配能力将来源菜单移动到与 `routeTarget` 匹配的目标路由下；来源菜单继续生成原 name/path 和按钮权限，但不再出现在普通 SideMenu。目标不存在、目标循环或目标无效时保留来源菜单原位置，避免配置错误导致入口丢失。

本次目标路由 name 为 `midhub/settings`。`概览`、`配置与用量`、`空间管理`、`用户管理`、`组织管理`、`角色管理` 分别由 owning module 的 `baseMenu.json` 声明目标和排序。项目端只消费目标菜单的权限过滤后 children；个人中心保持独立，通过头像菜单进入。以后来源菜单 code 变化时，无需修改布局代码。

实施入口：

- `src/utils/menu.ts`、`src/store/menu.ts`：`routeTarget` 菜单树重挂载、路由/面包屑/权限生成及安全回退。
- `src/layout/hooks/useProjectSecondaryMenuExtensions.ts`、`useBasicLayoutController.ts`：读取目标 children、设置可见性、激活态和首个可见叶子。
- `src/layout/shells/BasicLayoutShell.vue`、`ProjectLayoutPage.less`：项目 Header 入口、禁用收起、设置态顶部 Tabs 和 scoped 样式。
- `src/layout/components/BusinessApplicationSwitcher.vue`：在不复制 Store 逻辑的前提下支持 Header 呈现模式。
- `../modules/project-side-ui/baseMenu.json`、`../modules/authentication-manager-ui/baseMenu.json`、`../modules/jetlinks-space-ui/baseMenu.json`：声明设置目标、排序和用户可见名称。

业务模块接入步骤、配置契约和排查方式见[菜单路由目标与项目设置导航使用指南](./菜单路由目标与项目设置导航使用指南.md)。

验证覆盖菜单跨父级重挂载、缺失目标回退、循环保护、权限过滤后的目标 children、SideMenu 隐藏、Header 入口可见性、设置默认进入和项目/租户/应用壳层隔离；同时执行相关 JSON 解析、Vue 文件行数检查、定向测试与项目端构建。

## 风险与验证方式

- 切换应用时必须卸载上一轮服务端动态路由，避免旧应用独有路由继续可达。
- 快速或并发菜单请求只允许最后一次响应提交路由和权限。
- 子账号登录无业务应用时回到原项目入口；业务应用无菜单时按进入应用逻辑落到 `/403`。
- 静态验证包括 JSON 解析、Vue/TS 语法转换、`git diff --check`、Scope 聚焦测试和 Header 边界搜索；真实接口与视觉交互需在登录态浏览器中冒烟。

## 验证结果

- 本轮租户边界修复执行 `node --test scripts/project-runtime.test.mjs scripts/project-settings-navigation.test.mjs scripts/project-system-config.test.mjs`，20/20 通过；覆盖租户启动不初始化业务应用 Store、租户菜单 Scope 显式为 `false`，以及项目端、应用端继续初始化应用上下文。
- 本轮 `Application/index.vue` 的 SFC 脚本与模板解析通过，文件为 269 行；运行态静态扫描、业务应用上下文测试、应用访问测试和 scoped `git diff --check` 通过。按用户要求未执行 build、tsc 或 vue-tsc。
- `pnpm -F jetlinks-web-core test:application-access` 通过，覆盖 project storage 启用时的同源写入、跨 Origin bootstrap、project storage 关闭时的独立项目端同源切换、关闭时拒绝跨 Origin 应用域名，以及切入应用后保留原项目名并写入应用 `scope`。
- `node --experimental-strip-types scripts/test-business-application-context.mjs` 通过，覆盖有效/失效选择、查询参数接管、`sessionStorage` 读写，以及项目入口 Scope 不下发 `X-Application-Scope`。
- `pnpm -F jetlinks-web-core test:layout` 通过 7 项测试，覆盖显式 `front.layoutVariant`、项目 Scope、应用 Scope、项目运行态、`routeTarget` 跨父级重挂载、code 改名、排序、缺失目标回退和循环保护。
- 三个 owning module 的实际 `baseMenu.json` 合并验证通过；`midhub/settings` 的权限前动态配置子项按概览、配置与用量、空间、用户、组织、角色顺序得到 6 项；零动态设置菜单时 Header 设置入口隐藏，个人中心仍从头像菜单进入。
- `BasicLayoutPage.vue`、`BasicLayoutShell.vue`、`BusinessApplicationSwitcher.vue`、`ProjectSecondaryMenu.vue`、`ProjectConfig/index.vue`、`ServiceUsageSection.vue`、`ProjectUsage/index.vue` 通过 `vue/compiler-sfc` 解析、脚本编译和模板编译；三个菜单 JSON、四个中英文 locale JSON 与 core `package.json` 解析通过。
- `git -C jetlinks-web-core diff --check` 与 `git -C modules/saas-manager-ui diff --check` 通过；静态搜索确认 `X-Application-Scope` 只由菜单 API 边界和 `package.ts` project storage scope 注入。
- `node --max_old_space_size=8192 --max-semi-space-size=64 -e "process.argv.push('--module-name','saas-manager-ui'); import('vite').then(({ build }) => build())"` 通过，转换 11040 个模块；仅保留既有 CSS `//` 注释、大 chunk 和静态/动态导入警告。`pnpm -F jetlinks-web-core build -- --module-name saas-manager-ui` 在 Vite 7 CLI 下会因未知参数 `--moduleName` 提前失败，未进入代码编译。
- `node --max_old_space_size=81920 --max-semi-space-size=64 ../node_modules/vite/bin/vite.js build -- --module-name saas-manager-ui` 通过，转换 11086 个模块；仅保留既有 Rollup output、CSS 注释、大 chunk 和静态/动态导入警告。
- 项目端定向构建转换 7,825 个模块后，被既有 `ProjectConfig/components/AmapConfigEditDialog.vue` 引用 `saas-manager-ui/api/project.ts` 未导出的 `updateConsoleProject_api` 阻断；本次修改文件在阻断前均已完成转换。`pnpm -F jetlinks-web-core build -- --module-name project-side-ui` 另受 Vite 7 将参数转换为未知 `--moduleName` 的脚本问题影响。
- 布局与设置改造后的 Vue 文件均未超过 300 行：`BasicLayoutPage.vue` 为 26 行，`BasicLayoutShell.vue` 为 151 行，`BusinessApplicationSwitcher.vue` 为 111 行，`ProjectConfig/index.vue` 为 90 行，`ServiceUsageSection.vue` 为 104 行，`ProjectUsage/index.vue` 为 26 行。
- 已执行全量 `vue-tsc`，本次布局、菜单和项目设置相关文件未报告类型错误；全量检查仍被既有 `configs/plugin/*` 等类型债阻断。
- 真实登录态浏览器仍需补验：租户端刷新不应请求 `/business-application/me` 或 `/business-application/{id}`；项目端首次进入应请求 `/business-application/me`；进入应用后普通业务接口和菜单请求都应携带 `X-Application-Scope`，项目入口请求不携带该 header，切换应用后应进入目标应用第一个可访问菜单。

commit hash / Pull Request：pending。
