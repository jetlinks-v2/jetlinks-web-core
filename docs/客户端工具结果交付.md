# 客户端工具结果交付

> 状态：当前交付契约；标记为“目标契约/目标接入”的小节尚未实施，不参与当前运行时推断
> owning module：ui/jetlinks-web-core
> 主要入口：src/layout/components/AiChat/clientToolResultDelivery.ts
> 权威端口规范：modules/jetlinks-ai-agent/docs/help/general-agent-tool-definition-spec.md

## 1. 责任边界

客户端业务工具声明 typed producer/consumer port 与 typed result；共享 compiler 投影 routing/catalog，共享 Result Adapter 生成本次执行的
binding、范围、数量、完整性和 evidence。业务工具不上传会话文件、不拼物理路径、不组装 binding/evidence envelope，也不声明 renderer capability。

当前实现状态：

- canonical producer/consumer 由 typed definition 同源编译 `name/type/mediaType/shape`，consumer 额外保留
  `required/sourcePolicy`；新增或修改的业务工具只允许这条写入路径；
- `portVersion=ai-tool-port/v1`、`consumerPorts`、`producerPorts` 是 canonical wire；`accepts/produces/outputShapes/prerequisites` 仅由 compiler 单向生成兼容投影；
- contract 版本为 `ai-client-tool-contract/v2`，catalog 审计拒绝 canonical port 与兼容投影漂移；
- clientToolAggregatePresentation.ts 已删除，浏览器不生成、修补或改写 ECharts option；
- 后端 canonical presentation compiler 是制图决策和 option 生成的唯一真值源；
- jetlinks-ai-agent-ui 只校验和渲染 canonical source；
- PDF/Word/PPT 与浏览器预览消费同一 presentation source。

发布迁移期仅由 `clientToolDefinition.ts` 集中兼容存量 `name/optional/source` consumer：其中
`name` 只投影为 `accepts/prerequisites`，不猜测 `type/mediaType/shape`，不获得 typed resource authority。
producer 与 consumer 允许单侧先迁移；同一 consumer 声明或数组中混用 canonical/legacy 字段时 fail closed。
业务模块不得复制该 Adapter，存量调用方仍应分批迁移到完整 descriptor。

静态与动态边界：

~~~text
ProducerPort = name + type + mediaType + shape
ConsumerPort = name + type + mediaType + shape + required + sourcePolicy
RuntimeBinding/Evidence = port identity + execution path/ref, range, count, completeness and claims
~~~

Renderer capability 属于会话而非业务工具：

- mediaType/preferredInputShapes：consumer 兼容声明；
- supportsSessionFile/maxInlineBytes/defaultMode：传输能力；
- deliveryPolicy/narrativePolicy：交付体验策略。

## 2. 交付策略

当前 materializer 对单个未区分 audience 的大结果使用以下安全降级链：

~~~text
session-file → explicit safe inline → bounded preview
~~~

1. 会话文件可用且完整 source 未超过文件上限时，写入 session file 并返回 canonical binding。
2. 文件关闭、不可用、上传失败或内容超限时，只有 producer 显式声明的 modelSafeInline 且未超过 maxInlineBytes，才完整 inline。
3. 未声明安全 inline 或超限时，只使用 producer 提供的 bounded preview；preview 不升级为完整 downstream source。
4. 没有安全 preview 时只返回有界 summary 与 warning。
5. 上传取消直接返回 CLIENT_TOOL_ABORTED，不触发 inline 降级。
6. 上传失败先补偿未完成文件，再进入允许的降级路径；历史成功产物不删除。

交付层只处理序列化、容量、唯一命名、上传、取消、清理和 canonical result 装配，不读取业务字段猜语义，也不按 renderer、工具或页面选择策略。

该顺序只适用于未声明 audience 的发布兼容路径。canonical output 已将 grounding、展示和复用拆为三个
audience；materializer 只读取声明的 audience 与 capability，不根据 payload、工具名或 carrier 猜测。

### 2.1 Audience-aware delivery

| audience | canonical 内容 | 默认消费者 | 交付约束 |
| --- | --- | --- | --- |
| `model-evidence` | claims、criteria、cardinality、coverage、limitations 与有界安全样本 | 模型上下文 | 只允许 bounded model-safe inline；完整 rich records 默认不可见 |
| `client-presentation` | renderer-ready 表格、图、卡片或媒体 source | 当前会话 renderer | 可 client-only inline 或 session-file；默认不进入模型 |
| `reusable-source` | 完整、可复用、可校验的 canonical source | downstream consumer | 必须保持 exact identity/digest；bounded preview 不满足 |

audience 是 output projection 的可见性/交付属性，不是新的资源 owner。一个 source 可以产生多个 projection，但必须共享
同一 binding/source evidence identity；不得复制数据后生成互不关联的 Evidence。audience 不授予权限、absence authority、
完整性、Task 完成权或 presentation obligation，也不改变静态 `type/mediaType/shape`。
后端 terminal audit 直接消费 canonical Evidence/obligation，不把 `model-evidence` 当成新的审计真值源。

`auto` 按 audience 确定性决策，不使用全局 inline-first 或 file-first：

1. `model-evidence`：只 inline producer/server 已归一化且明确 model-safe 的有界投影；超预算时保留 claims、
   cardinality、limitations 与稳定 selector，不把完整结果塞入模型上下文。
2. `client-presentation`：renderer 兼容、内容通过安全校验且未超过当前 renderer/client 额度时可 client-only inline；
   需要复用、超限或 capability 指定 file 时使用 exact session-file/ref。两者都不可用时，optional 才能降级为
   bounded preview，required 必须保持 partial/open。
3. `reusable-source`：默认使用 exact session-file/ref；仅当 source 显式安全、有界且 consumer 接受 inline exact
   source 时允许 inline。preview、viewport 与 model sample 永远不能满足该 audience。

每次决策至少绑定 audience、requiredness、canonical port identity、实际字节数、安全结论、renderer capability、
delivery mode、binding/source identity 与 completeness。取消、上传失败、超限、文件不可用、malformed/ambiguous、
media/shape mismatch 或缺少 exact ref 继续 fail closed，不能拿另一 audience 的 projection 偷换 required source。

Web Core materializer 是 projection carrier 与 exact source identity 的唯一 owner。业务工具只声明 output audience 和
source；新 external binding 显式声明 `sourceDigestProfile=bytes-v1`，`sourceDigest=sha256:<hex>` 仅覆盖实际上传的
UTF-8 / NDJSON 字节，不经过 JSON 重序列化。inline binding 保留现有逻辑摘要；缺失 profile 的历史 external binding
保留原 JSON parse/stringify 校验。profile 不参与逻辑 Evidence 身份；partial、truncated、
bounded preview/model sample 不携带 exact digest，也不能满足 `client-presentation` 的 required obligation 或
`reusable-source`。Web Crypto 不可用时 canonical exact projection fail closed，released legacy 路径保持原有容错。

## 3. Artifact 与 Record Stream

AiClientToolRecordStream 处理记录流；AiClientToolArtifact 描述 producer 已准备好的 renderer-neutral source：

| 字段 | 语义 |
| --- | --- |
| content | 完整内容；只供 session-file materialization，不自动进入模型 |
| mediaType/fileExtension | 编码与安全扩展名 |
| outputShape/bindingName | producer-owned output identity |
| recordCount | 完整 artifact 实际记录数 |
| totalCount | 当前查询范围内已知总数；未知不猜 |
| displayedCount | 仅 source 本身为 partial 时出现 |
| modelPreviewCount | 内部模型样本数，不进入用户事实 |
| maxBytes | session-file 内容上限 |
| modelSafeInline/maxInlineBytes | producer 审核的安全 inline 及上限 |
| preview | producer 显式生成的有界预览 |

完整 source 12 条、模型样本 5 条时，用户事实仍是 recordCount=12、returnedCount=12、complete=true；
modelPreviewCount=5 只属于内部投影。只有实际 source 降级为 5/12 时才声明 displayedCount=5、complete=false、truncated=true。
viewport、分页和虚拟滚动数量不回写 source 完整性。

结果信息分为：

- userClaims：可进入用户回答和事实审计的权威事实；
- cardinality/coverage：record、bucket、measurement、sample 和覆盖范围等完整性元数据；除非业务 owner 明确声明其本身就是用户目标，否则不生成 headline claim；
- resources：runtime 可物化的 opaque descriptor；
- modelSample：有界结构样本，不升级为用户事实或 downstream source；
- runtimeDiagnostics：阈值、截断原因和传输状态。

### 3.1 静态端口与运行时物化载体

静态 output kind 描述 producer 交付给 downstream consumer 的逻辑资源，不由运行时载体反推：

- `artifact` 是文件型业务产物，compiler 固定生成 `type=artifact`、`delivery=file`，不能满足要求结构化 inline/typed source 的 renderer；
- renderer-ready 结构化结果集声明为 `recordSet`，compiler 生成 `type=structured-data`、`delivery=auto`，并保留 producer 声明的 `mediaType`、`shape`、`recordPath` 与 cardinality；
- `AiClientToolArtifact` 只是 renderer-neutral 的运行时 materialization carrier。结构化 producer 可以使用该 carrier 在 inline、session-file 与 bounded-preview 之间安全交付，但 canonical binding 仍来自静态 structured output，不能被重类型为文件型 artifact port。

required presentation 与 producer 的兼容性只允许基于 canonical `type/mediaType/shape/delivery` 判断。media type、shape、资源类型或交付能力任一不兼容，或候选 malformed/ambiguous 时必须 fail closed；诊断只报告 canonical 声明是否兼容，不参与工具选择、执行或终态裁决，也不创建第二套 routing/catalog/ledger。一个不兼容 producer 不得使无关 sibling 失效。

### 3.2 Required typed presentation 契约

- owner 与范围：`ui/jetlinks-web-core` 继续唯一拥有 typed output compiler、canonical binding 与 renderer-neutral
  兼容性诊断；业务 handler、接口、renderer、会话协议和后端 admission 不获得同类 owner。
- 当前合同：静态 `artifact` 固定编译为 `artifact/file`，renderer-ready 结构化结果使用
  `recordSet -> structured-data/auto`；required presentation 诊断只读取 canonical
  `type/mediaType/shape/delivery`，对 malformed、ambiguous 和不兼容候选 fail closed，并隔离 sibling。
  运行时 materialization carrier 不改变静态 output kind。
- 长期门禁：compatible、artifact/file、media/shape mismatch、malformed/ambiguous、sibling isolation、
  inline/session-file identity 等价及 bounded-preview fail-closed 必须由匿名合同覆盖；具体测试数量和构建日志留在 PR/CI。

### 3.3 Compatibility preflight 与 runtime admission

- catalog/registration 目标接入只使用 canonical `type/mediaType/shape/delivery/audience` 检查 descriptor 自洽和
  producer/consumer 静态兼容性，输出 typed diagnostic；它不执行工具、不选择本轮 producer、不关闭 Task，也不创建第二套 catalog。
- runtime producer/target port admission 仍由后端当前 Task、ToolSurface、Evidence 与 Delivery requirement 决定；
  前端 diagnostic 只能帮助发布前发现静态端口错误，不能成为 terminal verdict。
- materialization 在 handler 成功后根据实际大小、session-file 和 renderer capability 决定 delivery。运行时
  `AiClientToolArtifact` 等 carrier 不得反推静态 output kind，也不得将 file artifact 重类型为 structured source。
- `AiClientToolArtifact` 与 `AiClientToolRecordStream` 复用同一个 audience-aware decision。record stream 在 renderer
  inline 额度内保留 exact client-only source，实际大小越界时才切换 exact session-file；默认/显式 inline 的 `model-evidence` 不写文件，
  `reusable-source` 无 exact inline/ref 时保持 partial。NDJSON file 与 inline records 使用同一 actual-source digest，
  sample/preview 永远不升级为 exact binding。
- empty 只改变 Evidence/absence 语义，不授予 Delivery completion。canonical 空结果仍走同一 carrier admission：
  `model-evidence` 可 bounded inline empty；required `client-presentation` 必须获得 compatible inline 或真实 exact file/ref；
  `reusable-source` 默认仍需 exact ref，除非 consumer 明确接受 exact inline。空内容 digest、`totalCount=0` 或 sample path
  都不能替代 required carrier proof。
- 不兼容或 malformed candidate 只隔离自身；合法 sibling、已提交 Evidence 和无关 renderer 不受牵连。

### 3.4 完整结构化源先于模型视图交付

- 最终回复预算修复：artifact 本体可满足 renderer 的 inline 字节额度，但与 envelope、bindings 一起封装后仍可能超过同一 `replyMaxJsonLength` 字符额度。在既有 artifact 分支，对首次按声明选择 inline 的完整候选回复检查真实序列化长度；仅 `auto` 且原输出/renderer 均允许 session-file 时，沿原上传路径改用文件引用并重新合并。结构化源物化最终只执行一次。显式 inline、文件失败后的 fallback、model-evidence、取消和缺失能力保持原合同；不放宽 guard、不按业务或固定折扣阈值切换，不扩展 record stream。验证覆盖本体低于 inline 额度而整包越界、正常小包、上传失败/取消、显式 inline 与 capability 拒绝，并通过最终 guard 核对 exact ref、digest 和完整性。上传内容逐字保持，记录数、查询范围、默认数量和上限均不变。
- 本片阶段验证：沿既有 esbuild 配置编译两份定向测试 bundle，contract 8/8、preparation 3/3 通过，`git diff --check` 通过。最终 runtime 用例同时验证 native 的 exact ref/digest 保留、未落盘 model sibling 仍不完整、整体完整性不被抬高。首轮环境缺少既有 locale/browser 适配以及新增夹具变量和输出声明错误已修正，原失败日志保留；生产只改 artifact 分支。`vue-tsc --noEmit -p tsconfig.client-tools.json` 未通过，报告 158 项错误、没有落在本片三个代码/测试文件；未确认这些错误的完整旧版对照，不声明全量 typecheck、应用 build 或 coverage 通过。证据在 `/private/tmp/artifact-envelope-budget-fix-v1/`，当前未进行浏览器服务复测。

- 已授权合同 `typed-aggregate-carrier-before-view-v1`：由 `clientTools.ts` 传入同一有效 result guard 额度，
  `clientToolResultDelivery.ts` 只对显式 `auto/file` 的 canonical `structured-data` output 检查完整声明源。
  源超过该额度时，复用当前会话 sessionFiles 的能力、大小限制、取消和回滚，将 binding.path 选中的完整 JSON 保存。
- 成功后仅将同名 binding 的 `path` 载体替换为 `ref/sourceDigest`，保持 audience、shape、mediaType、逻辑
  `recordPath`、字段、排序、范围、recordCount、聚合 cardinality、coverage、claims 和 continuation；从回复移除该精确源路径。
  例如 `path=$.__clientToolOutputs.output0, recordPath=$.records` 保存的是 output0 本身，文件绑定仍使用 `$.records`。
- 该例外不创建用户请求的 artifact 或 terminal Delivery proof。真实 partial、不可读源、错误摘要、取消、上传失败、
  缺失会话能力或现有限额超限均不得升级为完整证据；剩余描述符超出额度时仍按原 guard fail closed。
- 后端 `EvidenceSourceVerifier` 通过当前会话读取并验证完整源，将有界、请求内的 resolved source 交给
  `ToolResultEvidence` / `StructuredResultEnvelope`，复用 inline 的字段、排序、范围、行数、覆盖和时间准入。
  不新增独立台账，不从工具名、业务字段名、样本或用户文本推断源。
- 阶段验证：`node ui/jetlinks-web-core/scripts/run-client-tool-contract-tests.mjs` 132 项通过，
  `node ui/jetlinks-web-core/scripts/run-client-tool-preparation-tests.mjs` 15 项通过；前者 esbuild/Node 覆盖率
  lines 97.59%、branches 88.20%、functions 92.23%。后端离线 Maven 编译与定向测试 48 项通过：
  `ToolResultEvidenceStructuredContractTest` 43 项，以及 `ToolOutcomeProcessorTest` 的完整源、会话/摘要、
  native admission 与失败边界 5 项。已覆盖 744 行完整源、末行错误、4097 行拒绝、计数不符、真实 truncated、
  inline/ref 身份等价、会话与字节限制、上传失败/取消回滚及混合 artifact sibling 隔离。
- 本阶段未执行全量前端 lint/vue-tsc/build、真实业务接口或模型调用；业务 resolver 的 production bridge
  适配与最终前后端整合验证另行执行，不能以本阶段合同测试代替。

### 3.5 外部源摘要 profile

- 已确认合同 `source-digest-profile-v2`：共享 materializer 为新 artifact、完整 structured spill 与 record stream 的
  external ref 声明可选 `sourceDigestProfile=bytes-v1`；摘要覆盖实际上传字节。JSON 空白、转义和数值表示不重写，
  NDJSON 包括每行换行及空流的零字节。inline typed source 与 native chart 的 `$.option` 继续现有逻辑身份准入。
- 后端 `ProducedOutputBinding`、`StructuredSourceDescriptor` 保留该 carrier 字段；`EvidenceSourceVerifier` 只按
  显式 profile 选择一次摘要算法。缺失 profile 保留 legacy 校验；不支持的显式值或 inline profile 必须拒绝，不能尝试
  多个算法或回落 legacy。历史 EvidenceIdentity 不重算，完整源仍进入现有 ResolvedSource 和 typed admission。
- 范围：共享客户端 binding/type/normalizer、delivery owner、后端 binding/parser/serialization/verifier 与对应测试；
  不增加依赖，不做 JCS、数字转换、迁移台账、业务特判或 previewHash 改写。
- 验证：前端 contract 135 项通过（lines 97.59%、branches 88.43%、functions 92.24%），preparation 15 项通过；
  后端离线 Maven 编译与定向测试 78 项通过。`ai-agent-general/src/test/resources/source-digest-bytes-v1.json` 由真实
  前端 materializer 捕获，常规合同测试再次核对实际上传内容；Java session verifier 消费同一 10 组向量，覆盖
  string/Blob/ArrayBuffer、科学计数、大数、负零、嵌套、转义、格式化 JSON、744 行源及 NDJSON 0/1/3 行。
  错字节不能获得 CONTENT，显式 bytes-v1 不能尝试 legacy；缺失 profile 的历史 JSON / NDJSON 行为保留，
  包括历史空 NDJSON 的解析拒绝。typed admission、inline/ref 逻辑身份等价、descriptor capture/read 和 profile 拒绝通过。
- native chart 当前返回 `$.option`，故继续缺省 profile；测试保留 previewHash/sourceDigest 对应关系、内容不可变与
  相同输入摘要一致性。session materializer 仍可为已准入 inline source 提供物理 backing，原 binding / EvidenceIdentity
  不改成新的外部 wire 身份。未知或 malformed 显式 binding 不得被同名静态 inline path 重建成 legacy binding。
- `vue-tsc --noEmit -p tsconfig.client-tools.json --pretty false` 退出 2，当前工作区有 157 条诊断，涉及账号 API、Captcha、
  routing / definition 等；未报告本次 `clientToolResult.ts` / `clientToolResultDelivery.ts` 的错误，整体类型检查仍未通过。
  前端合同中的 esbuild 编译通过；本阶段未执行全量 Vite build（待运行 `cd ui/jetlinks-web-core && pnpm build`），
  owning package 无独立 lint 脚本，未声称 lint 通过。未调用真实业务接口或模型。Maven 的历史 JaCoCo execution data
  有 class fingerprint 不匹配警告，本阶段不据此声明后端覆盖率。

## 4. Binding 标签、范围与 ordering

- 静态 name/shape/mediaType 保持 port identity，不因展示 label 改变。
- resolveLabel 只能消费 producer 已声明的 label、semanticRole、measure、aggregation 和 ordering。
- requestedRange/observedRange 随同一 binding 经过 inline、record stream、session-file 和 fallback，交付方式不改变覆盖判断。
- materialized output 未声明更具体范围时，继承本次 producer evidence 范围。
- ordering 只接受显式字段、方向和 producerGuaranteed；不从样本、字段名、工具 ID、页面或用户原文推断。
- 无效 ordering 整体 fail closed，但不损坏原始结构化 binding。

执行排序投影已在 `clientToolDefinition.ts` 的既有 output authoring 扩展点增加 typed `resolveOrdering`，
从本次执行结果向该 output 显式映射 producer-owned ordering。声明该映射的 output 不发布静态排序；映射返回
`undefined` 表示本次没有排序，不回落到请求参数或其他 binding。`jetlinks-ai-ui/agentCapabilities/aiSearch/tools.ts`
将同次 artifact 的 `ordering` 映射到模型证据 output，保留服务器已确认的完整排序键与方向；presentation 继续使用
同一 artifact。该映射不改变 HTTP、公共结果 schema、查询参数或 evidence scope，也不将 returned-window 升级为 population。

阶段验证：`visualSearchTransportContract.test.mts` 在真实 visual producer → source selector → factory → runtime binding
路径覆盖服务器默认排序、earliest/latest、服务器排序与请求方向不一致、合法执行无排序；模型证据与 presentation 保留
相同完整排序，静态 definition 无固定排序，returned-window 权威不扩域，以上断言均通过。
仅执行一次 `node ui/modules/jetlinks-ai-ui/scripts/run-passenger-flow-tests.mjs`，结果 53 项中 51 项通过、2 项失败：
既有升序用例仍期待单键，实际保留正确的 `eventTime,recordId` 两键；error adapter 的 optional recovery action 用例暴露
coarse-status base 的 `terminal` 在 typed failure 省略 action 时残留。阶段尚未通过，生产排序实现保持冻结。
升序旧断言已补齐执行结果的两键，并核对 presentation 一致性；此测试修订尚未复跑，等待 error adapter 合并缺口修复后统一验证。
完整日志：`/private/tmp/jetlinks-agent-review-20260907/execution-ordering-projection-v1/ui-stage-validation.log`；
SHA256：`4e7e0b55822758d6c69429a29588f891e307c763dbded8cffcb031710f5df92d`。
本阶段未执行 typecheck/build 或真实模型验证；独立验收仍需安排类型检查
`cd ui/jetlinks-web-core && ../node_modules/.bin/vue-tsc --noEmit -p tsconfig.client-tools.json --pretty false`。

## 5. 文件生命周期与补偿

默认文件名由稳定工具段、可读时间和 execution UUID 组成。call.id 只用于调用关联，不能承担跨 turn 唯一性；同一执行分块复用路径，不同执行永不覆盖。

内部状态为：

~~~text
not-started → unknown → opened → committed
                         ↘ rolled-back
~~~

- 请求发出但未确认时为 unknown，分片成功后为 opened，结果完成装配后才 committed。
- 只有 unknown/opened 允许幂等补偿删除。
- 用户主动删除保持严格 404；交付补偿使用 ignoreMissing=true。
- ignoreMissing 只容忍目标已不存在，权限、非法路径和存储错误仍传播。
- 未声明 audience 的发布兼容记录流在 count=0 时保持 inline empty 且不上传文件；canonical audience 若要求 exact
  session-file/ref，则上传真实零字节 NDJSON 并计算同源 digest，缺少 required carrier 时保持 partial/open。

已持久化 fs:// 引用只读恢复，不迁移、不双写。业务工具显式 writeToPath 时，覆盖/追加语义仍由该工具拥有，不被默认 materializer 接管。

## 6. 输入默认值与叙事策略

AiClientToolInput.defaultValue 是 provider-neutral 业务契约。clientToolRouting.ts 将其投影为 PropertyMetadata.expands.default，provider adapter 再处理模型方言；执行层仍使用同一业务常量做最终默认和值域校验。

Renderer 的 narrativePolicy 只指导正文组织，不改变权限、工具可达性、presentation obligation 或终态：

| mode | 默认职责 |
| --- | --- |
| card-first | 卡片承载明细时只保留摘要和可选下一步 |
| analysis | 允许基于 evidence 解释趋势、比较和异常 |
| free | 无卡片职责时保持普通正文 |

策略使用低基数 token 和有界上限；runtime 不因 narrativePolicy 改写模型正文。

## 7. 客户端动作确认与工具事实闭环（已实施）

### 7.1 目标与边界

目标是让客户端工具的声明、执行、确认和结果展示形成同一个可审计闭环：业务模块只声明稳定输入、typed
producer/consumer port、副作用和业务执行；共享 runtime 负责 catalog 快照、参数绑定、确认、执行状态和结果交付；会话 UI
只展示 runtime 已确认的状态，不把“用户已同意”误当成“工具已执行成功”。

影响范围与 owning module：

- `ui/jetlinks-web-core/src/layout/components/AiChat/`：客户端工具 authoring facade、catalog/snapshot、确认与执行编排、
  structured navigation/result adapter；
- `ui/modules/jetlinks-ai-agent-ui/components/AgentConversation/`：按 tool call id 关联确认和执行结果，展示完整状态；
- 首个业务 adopter 为 `ui/modules/device-manager-ui/agentCapabilities/deviceAnalysis/`，业务约束记录在该模块
  `Business.md`。

本计划不修改后端 FLAT `requestTools` 投影、不让前端按模型轮次筛选工具、不改变 canonical presentation renderer、
不改变设备查询接口和权限边界，也不复制实现到 `runtime-ui/`。FLAT 普通业务轮保持完整声明集属于后端独立修复；前端只保证
授权 catalog 的语义快照稳定、声明可编译、执行结果可追踪。

### 7.2 实施步骤

1. **收紧 authoring contract**：catalog 审计增加输入必需性与 consumer port 一致性检查。执行必须依赖某个逻辑资源时，
   consumer 使用 `required=true`；`sourcePolicy=EITHER` 继续允许该资源来自用户明确参数或前序 typed output，不把“可直接传参”
   错误解释成“逻辑依赖可选”。校验失败只拒绝当前 malformed tool，不隐藏有效 sibling。
2. **保持 READ 纯度**：READ 工具不得写 handoff、跳转路由或修改页面状态。查询结果只返回 typed data、cardinality、subject 和
   structured navigation receipt；安全链接和 Markdown 表现由共享适配层根据已授权菜单生成，业务结果不再携带 retry prompt、
   reply policy 或 instructive prose。
3. **建立通用 prepare-confirm-execute 边界**：为 WRITE/EXTERNAL_ACTION 提供可选的 typed prepare 阶段，在确认前解析并校验
   subject、生成有界确认摘要和 prepared arguments；用户确认后使用同一 prepared receipt 执行，并在必要时做最终重校验。
   prepare 失败直接返回结构化 request/permission failure，不弹出针对不存在目标的确认框。该能力是公共扩展点，不按工具 ID
   或设备场景分支。
4. **统一确认与执行状态机**：共享 runtime 按 call id 发布
   `pending-confirmation -> approved/executing -> completed|failed`，拒绝走 `rejected`；`approved` 只是中间态。
   AgentConversation 合并确认事件和 tool result，失败时展示用户可理解的对象与下一步，刷新/重连后仍按权威结果恢复，不把
   “已同意”渲染为终态。
5. **稳定 catalog 生命周期**：保留 registry revision、wire signature、执行快照和 active-execution 延迟发布；补充诊断记录
   catalog revision/signature、工具总数、malformed 数量和本次 call 所属 snapshot。业务 provider 不读取 WebSocket/session 字段，
   不自行创建 FLAT/HYBRID declaration epoch。
6. **收敛 failure/recovery**：参数无效、目标不存在、权限不足、依赖不可用和用户拒绝使用既有 typed failure disposition 与
   recovery action；修复建议通过字段、逻辑资源或 capability 表达，不在业务结果中写模型提示词。正文只能依据成功 tool result
   声称操作完成。

### 7.3 风险与兼容

- prepare 是公共 authoring contract 的可选新增阶段；存量工具保持原执行路径，不添加旧字段别名或双协议。首个 adopter 验证后
  再按工具族迁移，未迁移工具不自动获得“已预校验”语义。
- structured navigation 必须经过当前菜单授权解析；无法绑定已授权菜单时只返回业务 subject，不生成或猜测 URL。
- 确认状态需要与后端/客户端 tool result 的 call id 稳定关联；缺少最终结果时显示“状态未知/可重试”，不得推断成功。
- catalog 诊断只记录低基数 revision、signature、计数和工具 ID，不记录 token、完整参数、敏感结果或用户原文。

### 7.4 验证目标

- contract 单测覆盖 required consumer + `EITHER`、malformed sibling 隔离、READ 隐藏副作用拒绝或审计、prepare failure、
  用户拒绝、执行成功、执行失败和重校验失败；
- snapshot 测试覆盖同 schema handler 刷新、执行中 revision 延迟发布、刷新/重连后 call 使用原快照；
- AgentConversation 测试覆盖“已确认后成功”“已确认后失败”“拒绝”“刷新恢复”，确保 `approved` 不作为终态；
- owning module 定向测试覆盖模糊名称、显式 ID、typed lookup ID、无效 ID、无权限目标和单一/多候选；
- 阶段性统一运行 `jetlinks-web-core` contract/type tests、`jetlinks-ai-agent-ui` 定向测试、owning module 工具测试与窄构建，
  最后使用应用内浏览器验证完整确认和导航闭环；不在每个小步骤重复全量构建。

### 7.5 实施结果

- `clientToolDefinition.ts` 与 `clientTools.ts` 已增加只适用于 `WRITE/EXTERNAL_ACTION` 的可选
  `prepare` 扩展点；预校验参数在确认期间锁定，确认摘要有长度上限，存量未迁移工具保持原路径。
- `AgentConversation/clientToolConfirmationState.ts` 按 call/interaction 关联权威 tool result，将确认后状态区分为
  `approved/executing`、`completed`、`failed` 和 `rejected`；实时与历史恢复共用同一结算逻辑。
- 集中验证通过：`jetlinks-web-core` contract 60/60（行覆盖率 96.24%）、definition 类型检查、
  prepare 生命周期 3/3；`jetlinks-ai-agent-ui` 确认结算 4/4；`device-manager-ui` 工具契约 20/20；
  `device-manager-ui` 与 `jetlinks-ai-agent-ui` 窄构建通过。
- 9101 应用内浏览器已发起有效和无效设备目标的新会话；两次均长时间停留在后端“正在处理平台数据”，
  未产生客户端确认请求，因此真实导航闭环仍待后端/模型轮次恢复后复验；该环境阻塞不影响上述前端自动化门禁。

## 8. bounded analytical authoring 契约

### 8.1 当前结论与目标

Web Core 提供稳定、wire-free 的 analytical producer DSL。业务工具只声明自己的语义标识、事实族、subject、measure、dimension、
filter、criterion、coverage，以及 bounded criterion 对应的 semantic measure、physical value field、coordinate field、静态 axis 或
`axisFromInput`、`boundedBy` input 和 output binding；唯一 compiler 将这些通用事实映射为
`expands["x-ai-routing"].analyticalCapability` 和执行期 canonical output binding。`version`、`capabilityId`、`semanticKey`、
`transformCost`、wire completeness、`producerGuaranteed` 和 proof 生成均留在 Web Core adapter 内部，业务执行器只返回业务数据与
普通 success/partial 状态。

bounded completeness 相对本次 canonical requested scope 判断，而不是要求 producer 证明候选全集：只有普通 success、正的安全整数 limit、
limit 位于声明值域、显式选中的 output 为数组且返回数量不超过 limit 时，才保持 complete；真实 pagination/continuation、业务 partial、
缺失或非法 limit、output 未选中或绑定不唯一、返回数量越界均降级为 `analytical_scope_unproven`，不生成 authoritative complete evidence。

### 8.2 owner、范围与非目标

- 唯一 owner：`ui/jetlinks-web-core/src/layout/components/AiChat/clientToolDefinition.ts` 及其 public `clientToolApi.ts`；
  `clientToolRouting.ts` 只保留内部 wire DTO 与 canonical normalizer。
- adopter：`ui/modules/jetlinks-ai-ui/agentCapabilities/passengerFlow/tools.ts` 与
  `ui/modules/jetlinks-ai-ui/agentCapabilities/aiSearch/tools.ts` 只做机械迁移，不复制 DTO、compiler、proof 或 routing helper。
- 受保护范围：设备、客流、问图首页推荐 provider、locale、测试与 owning 文档原样保留；推荐文案不得被 routing、selection、
  completeness、Evidence 或 terminal policy 读取。
- 不修改业务 HTTP/transport/query/service、排序、limit 值域、菜单、页面壳、renderer、后端 wire、Task/Evidence/epoch/Delivery，
  不新增按业务名、工具名、关键词、字段 shape、fixture、prompt 或模型的分支。

### 8.3 当前 contract 与实现

1. Web Core 提供 generic analytical producer 与 bounded analytical producer 两种 typed helper。业务侧的 `producerKey` / `factKey`
   是稳定业务语义标识，compiler 分别映射到 wire identity；`coverage` 映射 wire completeness，`version` 与 `transformCost` 使用
   adapter-owned canonical 值。普通 producer 可直接声明 semantic ordering；bounded producer 只在 criterion binding 中声明一次
   measure、direction 与 physical value field，compiler 同时生成 semantic capability ordering 和 physical output ordering。
2. `boundedBy` 必须显式指向 `TArgs` 中的一个 input，output binding 必须显式指向同一工具的一个 output name。compiler 只按这两个
   显式标识解析物理端口；output shape 从已绑定 output 读取，bounded input 自动进入 wire filters，并在同一
   `analyticalCapability.completeness.boundedBy` 中投影为 `requested-record-window` 与精确 top-level `limitArgument`。禁止根据 filters、
   字段顺序、label、output shape 或数组位置猜配。
3. canonical output field 使用 `measure` 表达 measure identity，使用 `axis` 表达 dimension identity；二者都不得回退到物理字段名。
   bounded criterion 必须显式绑定一个已声明 measure、该 measure 的 physical value field、一个 physical coordinate field，以及唯一的
   static axis 或 `axisFromInput`。compiler 只接受精确一一映射；动态 axis 只能取自本次调用中显式绑定的 top-level enum input，enum
   值集合必须与 producer dimensions 完全一致，并在同一 `analyticalCapability.argumentBindings` 中投影为唯一
   `{ semantic: "dimension", argument }`。静态 axis 和普通 producer 不生成该 binding；禁止从字段 shape、label、数组位置或样本值猜测。
4. 通用 time-scope helper 同时返回一个 opaque temporal authoring edge；业务工具通过已有 wrapper 组合它，不重复声明参数名或 wire。
   analytical compiler 仅在 typed inputs、closed alternatives 与 custom branch 共同证明唯一映射时，向同一
   `analyticalCapability.argumentBindings` 投影
   `{ semantic: "temporal", rangeArgument, startArgument, endArgument, customValue, encoding: "date-time" }`。
   server 只把 admitted Task 的 absolute range 以 ISO-8601 instant 写入已声明 custom branch；不反推 preset，不发送 timezone/grain。
   缺失、重复或冲突 target，custom required/const 不一致，start/end 不是 `date` typed input，或存在多个无附加前提的 custom branch，
   整个 analytical descriptor 局部 fail closed。
5. 通用 scope helper 组合 canonical consumer port、top-level optional string input 与 closed input alternatives，并返回 opaque scope
   authoring edge；业务工具不构造 wire DTO。analytical compiler 只在 project/area/point 三个 coordinate 各自唯一、branch required/forbidden
   完整且 target 不与 dimension/limit/temporal 冲突时，向同一 `analyticalCapability.argumentBindings` 投影
   `{ semantic: "scope", valueCardinality: "exactly-one", encoding: "single-string", coordinates }`。project 必须是显式
   `arguments=[]` 的 omission branch；area/point 必须分别引用唯一 canonical consumer `sourcePort` 与唯一 top-level string argument。
   缺 Evidence 或缺 binding 不得默认成 project；缺失、重复、端口不匹配、branch 不闭合或 target 冲突时 descriptor 局部 fail closed。
6. bounded helper 返回 opaque authoring handle，不暴露 capability DTO 或 proof API。执行器只返回业务数据和普通 success/partial；
   Web Core 在同一次 compiled tool 调用内自动绑定 declared scope、canonical limit、selected output、criterion 和 runtime axis。只有
   success、有效正整数 limit、已选中目标数组且数量不超过 limit，并且 measure/value/coordinate/axis 映射全部有效时，才保持
   requested-scope complete；其他情况统一 fail closed。
7. 删除当前未发布的 raw `AiClientToolAnalyticalCapability` public export、`defineClientToolAnalyticalCapability`、
   `defineClientToolBoundedScope`、`ClientToolAnalyticalCompletionProof` 与 `analyticalCompletion` result option，同步迁移全部 AI UI
   analytical adopter，不保留同 PR 过渡别名。已发布 legacy name/effect/typed/routed/plain/remote-adapted 工具格式继续走现有兼容 owner。
8. 真实 pagination/continuation、业务 partial、64 KiB 且无 exact ref、malformed/ambiguous/unselected、显式 binding 不匹配和
   canonical/legacy conflict 继续局部 fail closed；一个 malformed sibling 不得使合法 producer、catalog 或 session 失效。
9. `preparation: true` 是仅存在于 authoring 输入的 bounded compile-time sugar；Web Core compiler 只把它映射为既有
   `x-ai-routing.stages=["preparation"]`，不写入 compiled metadata、session 顶层或第二份 role，业务工具也不得直接填写 raw routing。
   output kind 继续描述资源/载体而不反推 lifecycle：包含 record-set 的 scope resolution 仍可声明为 preparation。preparation 不得同时
   获得 analytical producer authority，compiler 对这种冲突 fail closed；未声明 flag 的已发布工具继续使用既有 output-kind 默认 stage，
   保持 wire 兼容。
10. canonical consumer port 可通过 opaque typed authoring 将 catalog value 绑定到一个精确 top-level string input；compiler 只在 source
    port descriptor 与 input identity 都唯一且类型兼容时，在同一 `consumerPorts[]` 元素投影
    `argumentBinding={ argument, valueCardinality: "exactly-one", encoding: "single-string", contextSource? }`。`contextSource`
    只能由 opaque primary-subject authoring 投影为 `{kind:"subject", selection:"primary", subjectType, coordinate:"id"}`：
    已声明 `argumentBinding` 时，`sourcePolicy=TOOL` 禁止它，`CONTEXT` 必须具备，`EITHER` 仅在显式声明时授权 context
    branch；未绑定 native argument 的既有 CONTEXT resource port 保持原语义。port 的
    `name/type/mediaType/shape/sourcePolicy` 继续拥有 source identity；`valueCardinality` 只描述一次 native call 写入一个 scalar，不限制 catalog
    Evidence 可包含的候选数量。缺失、重复、非 string target 或与其他 semantic target 冲突时该工具局部 fail closed，不从字段名、shape、
    label、数组位置、tool 名或业务词推断参数或 subject type。
11. 非 analytical catalog 工具复用同一个 time-scope opaque authoring edge，并只在现有 `x-ai-routing` root 投影 singular
    `temporalArgumentBinding={ semantic: "temporal", rangeArgument, startArgument, endArgument, customValue, encoding: "date-time" }`；
    analytical producer 仍只在 `analyticalCapability.argumentBindings` 持有 temporal。两处同时声明、custom branch 漂移、非 date endpoint
    或与 consumer/bounded/analytical target 冲突均 fail closed；不新增第二 metadata owner、版本、preset 反推或模型 exact-call relay。

### 8.4 验证矩阵与剩余风险

- Web Core 匿名合同覆盖 canonical `axis` 归一化、非法 axis fail closed、static axis、两种 runtime axis 共用同一 physical coordinate
   field、criterion measure 与 physical value field 精确映射、semantic/physical ordering 一致、bounded limit 与 dynamic dimension 的
   typed wire 投影；temporal 矩阵覆盖 custom date-time 正例、missing/duplicate/conflicting target、custom required/const mismatch、
   非 date input、重复 custom branch、unknown semantic 与 cross-domain sibling 隔离；并继续覆盖缺失 argument、schema/dimensions 不相容和
  unselected/mismatched mapping 不产生 authoritative completeness。
- bounded Top-N 继续覆盖 `< limit`、`== limit`、候选总体 `> limit` 但请求仅为 Top-N 和 criterion sibling；pagination、64 KiB
  oversize、exact ref、malformed sibling、canonical/legacy conflict 与 legacy name/effect/typed/routed/plain/remote-adapted 回归不放宽。
- AI UI 只机械迁移 analytical 声明和 output field metadata；真实业务 HTTP/transport/query/service/page-shell 不变。设备、客流、问图
  首页推荐 provider、locale、测试和 owning 文档完整保留，也不得被 routing/completeness 读取。
- production-chain 评测必须从真实 module loader/extension/runtime 取得同一个 compiled definition，并通过
  `handleClientToolCall` 进入 result adapter 与 Delivery；只允许替换外部业务数据访问。重新调用业务 factory 后直接 `execute`、手写同形
  producer/Evidence/claim 或固定终态正文只能作为局部单测，不能证明真实 session contract。
- 阶段边界集中执行 Web Core contract/preparation/type tests、AI UI 合同测试、聚焦 typecheck 与 production build；全量 `vue-tsc`
  只与可复现基线比较 no-new-regression。后端 canonical presentation/sourceEvidence/derivedClaim wire 不属于本阶段，前端不发明第二套协议。
- 2026-08-21 当前源码验证：Web Core `pnpm test:client-tools` 93/93、`pnpm test:client-tool-preparation` 6/6；
  AI UI `pnpm test:passenger-flow` 29/29。Web Core `pnpm test:client-tool-types` 与 AI UI 模块级
  `pnpm exec vue-tsc --noEmit -p modules/jetlinks-ai-ui/tsconfig.json --pretty false` 分别复现 150 / 699 条工作区既有跨模块基线错误，
  本切片 `clientToolDefinition.ts`、`clientToolRouting.ts`、对应 type/contract tests 与 `passengerFlowContracts.test.mts` 均为 0 条聚焦诊断，
  无新增回归。
  Web Core `pnpm build` 完成 22,485 个模块转换，`pnpm build:modules jetlinks-ai-ui` 完成 9,002 个模块转换；构建仅保留
  既有资源解析、Rollup output、CSS 注释和大 chunk warning。两个 owning 子仓库 `git diff --check` 通过。真实页面会话的
  producer selection 及后端
  presentation/sourceEvidence/derivedClaim 接线仍需在对应 wire 交付后另行验证，不能由本合同层猜造。

### 8.4.1 canonical timestamp encoding（2026-08-22 已实施）

目标是在现有 canonical output field owner 内冻结时间值编码，使后端无需根据字段名、数值范围、运行样本或业务工具推断
seconds/millis/string。`AiClientToolCanonicalOutputField` 仅为 `type="timestamp"` 接受必填
`encoding="epoch-millis"|"date-time"`；其他 canonical type、legacy field 和未知 encoding 一律 fail closed。该字段继续随同一
output contract、runtime catalog 和实际 result binding 贯通，不新增 metadata root、版本、owner 或兼容双轨。现有
WebSocket session serializer 仍依合同排除 browser-only `clientToolContract/resultBindings`；本次不为 encoding 新建第二个
session wire，服务端从工具实际结果的 StructuredBinding 消费同源 fields。

影响范围与 owner：

- `ui/jetlinks-web-core/src/layout/components/AiChat/clientToolResult.ts`：扩展 canonical field type、唯一 normalizer 与通用值校验；
- `ui/jetlinks-web-core/src/layout/components/AiChat/clientToolDefinition.ts`：在既有 result adapter 中按已声明 encoding 校验实际 selected
  records，不从样本反推 encoding；
- `ui/jetlinks-web-core/tests/clientToolContract.test.ts` 及必要 type test：覆盖 descriptor/wire/value 正反矩阵和 sibling isolation；
- `ui/modules/jetlinks-ai-ui/agentCapabilities/passengerFlow/tools.ts`：`flow_passenger_trend` 与同样返回 number timestamp 的
  `flow_portrait_distribution` 机械声明 `epoch-millis`；
- `ui/modules/device-manager-ui/agentCapabilities/deviceAnalysis/tools.ts`：online-rate/message trend 机械声明 `epoch-millis`；
- 相邻 production contract tests 只验证真实 definition → compiler/runtime/session 与 result adapter，不复制业务 definition。

实施边界：不修改业务 service/query/HTTP、页面、renderer、后端 StructuredBinding、analytical capability、axis/measure/unit/ordering、
recordPath、Delivery 或现有 temporal argument encoding。实际记录中存在的 `epoch-millis` 值必须是 finite integral number；数字字符串、
小数、NaN、Infinity 均拒绝，也不做秒尺度猜测。存在的 `date-time` 值必须是带 `Z` 或显式 offset 且可解析的字符串；无 offset、数字或
模糊日期拒绝。`epoch-millis` 的整数还必须位于 JavaScript safe-integer 范围内，避免已丢失精度的 Number
与后端 long/BigDecimal 产生身份漂移。字段值缺失仍表示该记录未提供该字段，不被转换成另一种编码；业务需要该值作为 analytical axis 时继续由既有完整性/
绑定门禁决定是否可用。

风险与阶段验证：新增必填 encoding 会使所有未迁移 canonical timestamp descriptor 局部失效，因此先以全 `ui/` 扫描确认 adopter；当前
生产扫描只发现上述 passenger/device 四处，测试 fixture 同步迁移。阶段边界集中运行 Web Core client-tool contract/preparation/type tests、
passenger/device production contract，随后运行 Web Core、AI UI、Device UI production build 与各 owning repo `git diff --check`；不访问
浏览器、7100/9101，不执行 commit/push/PR。完成后仅在本节回填计数、源码锚点、scoped fingerprints 与未验证风险。

实施结果：

- `clientToolResult.ts` 已将 canonical field 收敛为 discriminated union：`timestamp` 必须携带闭集 encoding，
  非 timestamp 和 legacy field 禁止携带。唯一 normalizer 不从 `format="datetime"`、字段名或样本值反推编码。
- 既有 result adapter 在 output `select/resolveFields` 后校验存在的时间值：`epoch-millis` 仅接受 safe integral
  JavaScript number，数字字符串、小数、NaN 和 Infinity 均 fail closed，且不按位数/值域猜 seconds 或 millis；
  `date-time` 仅接受带 `Z` 或显式 offset 的可解析字符串。malformed producer 仅失败本 sibling。
- production adopter 全局扫描只发现四个 canonical timestamp：`flow_passenger_trend`、
  `flow_portrait_distribution`、`device_query_online_rate_trend`、`device_query_message_trend`；均依真实 callback
  number 值声明 `epoch-millis`，不改原有 axis/measure/unit/ordering/analytical capability。
- 阶段门禁：Web Core client-tool contract 105/105、preparation 6/6，AI UI passenger production contract 29/29；
  Web Core、AI UI、Device UI production build 分别转换 22,486 / 9,003 / 9,465 个模块并通过。
  `test:client-tool-types` 仅复现已知工作区跨模块基线，本切片 production/type-test 路径无新诊断。
- Device 现有 agent runner 为 21/22；唯一失败是既有 property aggregate longitude/latitude 字段 stale oracle，
  与 timestamp encoding 及本切片四个 production adopter 无关，未为追绿修改 production declaration。根 UI
  `clientToolCatalogCoverage.spec.ts` 直接 Node strip-types 观察被 `node_modules/@jetlinks-web/constants/index.ts` 的
  `ERR_UNSUPPORTED_NODE_MODULES_TYPE_STRIPPING` 阻断，归类为 INVALID_FIXTURE；真实 definition/runtime/result-adapter 断言已写入该
  production contract test，但不将未执行的 root runner 冒充通过证据。

### 8.4.2 canonical scalar summary binding 实施计划

目标是让 standard analytical summary 的实际标量结果通过既有 output contract 产生 server-verifiable
StructuredBinding，而不依赖 callback 内嵌 claims、字段名猜测或模型复写。owning module 为 Web Core
`clientToolResult.ts` / `clientToolDefinition.ts` 的 canonical fields 归一化、实际值适配与 analytical compiler，
业务端仅在 AI UI passenger-flow 的现有 output 声明上补齐真实物理映射。

实施范围：

- 通用 scalar validator 按 canonical physical `type` 校验已解析 record；非集合路径解析出的单个 object 是 scalar
  binding，其 measure 必须全部存在且类型精确。集合 binding 允许稀疏行，但已出现字段仍必须匹配声明类型；integer 使用
  safe-integer，number 使用 finite number，不做字符串转换或样本推断。
- `criteria=["summary"]` 的 standard analytical producer 一旦声明 canonical fields，compiler 必须证明 capability 每个
  measure 与 output 中唯一 numeric measure field 的 `measure/unit/aggregation` 精确一致，且禁止缺失、多余、重复或无 label
  的 scalar measure。现有 time-trend 保持同样的精确 measure 门禁；未迁移且没有 canonical fields 的 summary，以及非 time
  analytical rowset，不由字段名或样本自动升级。
- `flow_passenger_summary` 只为真实 callback object 声明 `recordPath="$"`，并映射
  `inCount/outCount/totalCount/netCount/recordCount` 到既有
  `passenger_in/passenger_out/passenger_total/passenger_net/source_record_count`；保持 `criterion=summary`、
  无 ordering/time axis/trend/chart 语义。

非目标：不修改后端、页面、renderer、service/query/HTTP、callback claims 或 analytical metadata root/version；
不为 summary 新增 trend/presentation 能力，不将 raw key/shape/tool name 作为 semantic authority。

阶段验证覆盖匿名 standard summary 的 valid/missing/wrong-type/duplicate/undeclared-measure/sibling isolation，以及真实
`flow_passenger_summary` definition → compiler/runtime → result adapter 的 recordPath、fields 与标量值类型；集中执行
Web Core client-tool contracts、passenger production contracts、必要 build 与 scoped diff-check。timestamp slice 的其他两个
module build 在接口未变时直接复用，不重复验证。

实施结果：`flow_passenger_summary` 的 production definition、runtime registry 与 actual result adapter 现在共享
`recordPath="$"` 及五个 canonical integer measure fields；actual adapter 在 callback 不提供 claims 时仍生成同源
`__clientToolOutputs.output0` 与 output binding。缺任一 measure、numeric string、unsafe integer 或非 finite number 均在 selection
边界 fail closed，malformed 调用不污染随后合法执行；`flow_operation_summary` 与既有非 time temporal-profile sibling 不被隐式升级。

阶段门禁：Web Core client-tool contract 107/107、AI UI passenger production contract 30/30。Web Core / AI UI 全量类型检查分别
复现既有 150 / 699 条跨模块基线诊断，本节涉及的 production 与 test 路径均为 0 条诊断。因本节未变更 public type、build graph、
页面或 service/query，production build 复用 8.4.1 已记录的 Web Core 22,486 与 AI UI 9,003 模块通过证据；本节不重复构建，且未访问
浏览器、7100/9101，也未执行 commit/push/PR。

### 8.4.3 device trend root record binding 实施计划

目标是让设备在线率与消息趋势的真实 selected result 通过既有 output contract 发布 exact record root，使服务端只依
StructuredBinding 即可定位指标记录，不根据 output shape、字段名、工具名或运行样本猜测路径。两条 production callback 都返回
`data.points`，现有 `selectMetricPoints` 将该数组选为 output value，因此 canonical `recordPath` 是 selected value 的根路径 `$`，
不是原始 HTTP 响应路径 `$.data.points`。

影响范围仅为 `ui/modules/device-manager-ui/agentCapabilities/deviceAnalysis/tools.ts` 中
`device_query_online_rate_trend` / `device_query_message_trend` 的既有 aggregate-series output 声明，以及相邻 production contract
断言；Web Core output owner、wire root/version、service/query/callback、analytical capability、fields、timestamp encoding、axis、
measure/unit/ordering 均不改变。真实 definition → compiler/runtime/session 与 actual result adapter 必须保留同一 `recordPath="$"`；
missing、非法 path 与 malformed sibling 继续由既有共享合同局部 fail closed。

阶段边界集中运行 Web Core client-tool contract、真实 device production contract 与 scoped `git diff --check`。本切片不改变 public
type 或 build graph，若 focused 合同与类型诊断无新增回归，则复用 8.4.1 已记录的 Web Core / Device UI production build 证据；不访问
浏览器、7100/9101，不修改后端，也不执行 commit/push/PR。完成后只在本节回填验证结果、源码锚点与 scoped fingerprint。

实施结果：两条真实 aggregate-series output 均在原声明 owner 增加 `recordPath="$"`；既有 timestamp encoding、canonical fields、
analytical capability 与 ordering 未改变。相邻 production contract 已分别断言 compiled definition 的 `resultBindings`、runtime/session 中
同源 `clientToolContract.outputs`，以及 deterministic request adapter 执行后的 `outputBindings` 均保留根路径 `$`，并继续检查 selected
records 来自 `__clientToolOutputs.output0` 的根数组。

阶段门禁：Web Core `pnpm test:client-tools` 107/107 通过，包含显式 recordPath、缺失/非法 path 与 malformed sibling isolation；两个 owning
repo 及根 UI 目标测试文件的 `git diff --check` 通过。Device `pnpm test:agent-tools` 仍为既有 21/22 基线，唯一失败是 property aggregate
longitude/latitude stale oracle，与本节两条 trend output 无关。根 UI production-composed 断言未冒充已执行：当前工作区没有
`vite-node`，使用 production Vite SSR loader 可转换 154 个模块，但被 federation 虚拟依赖 `__federation_fn_satisfy` 阻断，归类为
INVALID_FIXTURE；按装置止损约束不增加第三套 mock。由于本节未修改 public type、build graph、service/query 或页面，production build
复用 8.4.1 已记录的 Web Core 22,486 / Device UI 9,465 模块通过证据；未访问浏览器、7100/9101，也未执行 commit/push/PR。

### 8.4.4 materialized structured-source selection 实施计划

目标是让 structured output 的 canonical fields 与 `recordPath` 始终校验同一个 logical selected source，而不是把运行时
`AiClientToolArtifact` carrier descriptor 误当成业务记录。`recordPath` 继续相对 materialized JSON source：selector 已返回数组时使用
`$`；carrier 的完整 source 是含记录集合的 object 时保留对应的 `$.<collection>`。compiler/result adapter/materializer 不根据工具名、
shape、raw key 或样本推断路径。

影响范围与 owner：

- `ui/jetlinks-web-core/src/layout/components/AiChat/clientToolDefinition.ts`：output selection 遇到已声明 materialized artifact 时，从共享
  materializer owner 取得 exact JSON source，再按静态 output 的 canonical fields / recordPath 校验；普通 inline selected value 保持原顺序。
- `ui/jetlinks-web-core/src/layout/components/AiChat/clientToolResultDelivery.ts`：复用 artifact content 的既有 JSON/source owner，提供有界 exact
  source 解码入口；不新增 routing、metadata root、版本、ledger 或 fallback。静态 path 已声明时，runtime carrier 缺省可继承，显式冲突则
  fail closed。
- Web Core 匿名合同覆盖 object-root artifact 的 inline/session-file 正例、缺失路径、字段类型错误、carrier path 冲突、非 JSON source 与
  valid sibling isolation；大屏 production contract 继续走真实 definition → compiler → runtime/session → callback → result adapter →
  materializer，证明 `$.results` 只作用于 renderer source。AI Search 作为同 carrier sibling 保持既有 delivery，不因本修复被升级或改写。

非目标：不把大屏 `recordPath` 改为 `$`，不修改 template query、finalizer 业务结果、carrier payload、renderer、页面、后端或其他工具；不在
业务工具增加选择 fallback，也不放宽 canonical field 类型校验。主要风险是 Blob/ArrayBuffer/string JSON source 的解码一致性，以及 malformed
carrier 在 materialize 前必须保持原 `CLIENT_TOOL_OUTPUT_SELECTION_FAILED` fail-closed 语义。

阶段验证集中执行 Web Core client-tool contract/preparation/type diagnostics、大屏 typed presentation production contract、AI Search artifact
delivery sibling、scoped `git diff --check`；仅当共享 public type 或 build graph 实际变化时重跑对应 production build，否则复用当前同源构建
证据。全程不访问浏览器、7100/9101，不修改后端，也不执行 commit/push/PR。

实施结果：

- `clientToolResultDelivery.ts` 在既有 artifact source owner 中提供 exact logical JSON source 解码，string、Blob 与
  ArrayBuffer 共用同一 bytes 归一化路径；解码失败、非 JSON media type 不产生伪 source。
- `clientToolDefinition.ts` 对带 canonical fields 的 materialized artifact 先取 logical source，再使用静态 output
  `recordPath` 校验 fields；carrier 显式 path 与静态 path 冲突时沿用 `CLIENT_TOOL_OUTPUT_SELECTION_FAILED`
  fail closed。普通 inline selected value 的选择与校验顺序不变。
- 大屏 production definition 仍声明 `recordPath="$.results"`；real callback 经 compiler、runtime/session、result adapter 后得到
  含 `id/name/analysis` canonical fields 的 inline presentation source，没有对 carrier descriptor 重复执行 selector。
  `ui/` 生产扫描中另一 artifact carrier 为 AI Search；它保留运行时 materializer 拥有的 path/delivery，
  本次不为其猜造静态 fields、recordPath 或其他 presentation 语义。
- 匿名合同覆盖 JSON string/Blob/ArrayBuffer carrier、ordinary inline、路径缺失、字段类型错误、非法 JSON、
  media type/path 冲突、malformed/valid sibling isolation；Web Core client-tool contract 108/108、preparation 6/6 通过。
  大屏 production-composed 断言经仓库现有 esbuild 边界运行通过；临时目录镜像只恢复测试源码 locator 的
  真实 workspace 相对层级，不改变 production 或 fixture 语义。
- Web Core production build 转换 22,486 个模块并通过；`test:client-tool-types` 仅复现既有 API/Captcha/
  页面/主题别名等跨模块基线诊断，本节 production/test paths 为 0 条诊断。构建仅保留既有资源解析、
  dynamic chunk、CSS 注释和大 chunk warning。

### 8.4.5 typed semantic intent binding 实施计划

目标是在既有 `expands["x-ai-routing"].analyticalCapability`、`analytical-capability/v1` owner 内建立
同一工具声明的自然语言 intent 与 analytical coordinates 之间的显式 typed edge，使服务端可以从已承认的 intent
唯一选择 criterion、measures 与 dimensions，而不让模型复写 criterion、时间范围或 producer 参数。候选 canonical
字段为 `semanticIntentBindings`，每项只包含 `intent`、单一 `criterion`、非空 `measures` 与非空 `dimensions`；
`intent` 必须精确引用同一 declaration 已发布的 `description.intents` 成员，其余坐标必须精确属于同一
analytical capability。subject、temporal/timezone/grain/order 与 Delivery 继续由既有 capability 和服务端规范化，
不在 binding 中复制。

影响范围与 owner：

- `ui/jetlinks-web-core/src/layout/components/AiChat/clientToolRouting.ts`：在现有 analytical capability type、唯一
  normalizer 与 session routing root 中贯通 typed binding；不新增 metadata root、版本或平行 contract。
- `ui/jetlinks-web-core/src/layout/components/AiChat/clientToolDefinition.ts`：扩展既有 analytical authoring/compiler，
  只投影业务显式声明的 intent relationship，并在唯一 compiler 中验证 intent 与 capability coordinates 的同源性。
- Web Core contract/type tests：覆盖 definition → compiler → normalized registry/tool-list → session serialization，
  以及 missing/duplicate/unknown/cross-sibling/capability drift 的 fail-closed 与 sibling isolation。
- `ui/modules/jetlinks-ai-ui/agentCapabilities/passengerFlow/tools.ts` 与
  `ui/modules/device-manager-ui/agentCapabilities/deviceAnalysis/tools.ts`：仅为真实 passenger trend、device online-rate
  trend、device message trend 的现有 analytical authoring 机械声明各自 intent、measure 与 time dimension；相邻 production
  contract tests 从真实 definition/registry/session 断言 wire，不复制同形 DTO。

非目标：不新增 Task、intent snapshot、workflowRole、selectedDimension、第二 owner/ledger/verdict/fallback；不修改后端、
页面、HTTP/query/service、renderer 或 locale 文案语义；不根据工具 id、自然语言相似度、字段/raw key、shape、catalog
顺序自动推断 binding。summary/rank/distribution/comparison/composition 等 sibling 不会因为存在相似 intent 或 capability
而被升级；未声明 binding 的 legacy/static capability 保持既有 wire 兼容，但不获得 direct-admission authority。

实施步骤与 fail-closed 边界：先冻结 compile-time-only authoring sugar 与 canonical wire；再由 compiler 验证 intent
非空且唯一、精确属于同一 description intents，criterion/measures/dimensions 非空去重且属于同一 capability，禁止两个
binding 争用同一 intent；normalizer 对 malformed、未知坐标、重复 semantic 或跨 sibling 残留返回无合法 capability，
不局部猜测或兼容。随后机械迁移三条真实 trend producer，并证明普通 sibling 不发布 typed binding。

风险与阶段验证：主要风险是共享 dirty tree 中已有 analytical/temporal/scope/output hunks，以及 description intents
经过 i18n 实例化后的 exact identity；实现必须叠加而不覆盖既有改动，并把 binding 始终绑定到编译后同一 declaration 的
实际 intent 值。阶段边界集中执行 Web Core client-tool contract/preparation/type tests、AI UI passenger production
contracts、Device UI production contract，随后运行三个 owning project 的 focused type diagnostics、production build 与
scoped `git diff --check`。不访问 browser、7100/9101，不修改 runtime-ui/backend，也不执行 commit/push/PR；完成后仅在
本节回填 canonical JSON、源码锚点、测试计数、scoped fingerprints 与剩余 backend consumer 风险。

`flow_passenger_summary` 的后续 scalar intent 切片继续复用本节唯一 owner。实施前只核对同一 production
declaration 已发布的 `description.intents` 和 capability coordinates：每个 exact intent 显式绑定
`criterion="summary"`、`passenger_in/passenger_out/passenger_total/passenger_net` 四个用户语义 measure
与 `dimension="direction"`。`area/point` 仍由既有 scope binding 所有，temporal 仍由既有 absolute-range binding
所有；`source_record_count` 仅保留在 capability/output/Evidence 中作 completeness 与诊断度量，不进入
Task requested measures。生产修改只触及 passenger-flow 现有 analytical authoring 与相邻 production contract
断言；Web Core 唯一 compiler/normalizer 不改，继续由已有匿名矩阵保证 missing/duplicate/unknown/
cross-sibling/capability drift fail closed。阶段边界集中运行 Web Core focused contract、passenger production
contract、AI UI changed-path type diagnostics/build 与 scoped diff-check；不修改 backend、页面、renderer、service/HTTP
或 locale 文案语义。

`scopeSelection` 增量切片仍由本节同一 `semanticIntentBindings` owner 承载。目标是让已有 typed intent
显式声明 scope 选择允许 `project` 或 `explicit`，不把缺失 scope Evidence 默认为 project。canonical
子契约为 `scopeSelection={binding:"scope",modes:["project","explicit"]}`，其引用必须在同一 capability
的 `argumentBindings` 中精确找到唯一、已通过 closed project/area/point、`exactly-one`、`single-string`
校验的 scope binding。

影响范围只限 Web Core 现有 analytical intent type/compiler/normalizer/session tests、passenger summary 的现有
production authoring 与真实 registry/session contract test。compiler 只投影业务显式声明的引用；missing/
duplicate/foreign/unknown mode/cross-sibling/collision/schema drift 均使该 capability 局部 fail closed。无 scope binding
的 trend/device/static sibling 不发布 `scopeSelection`，既有 dimension/temporal/limit/scope argument bindings 不变。
非目标为 backend model schema/Task 生命周期、文本解析、业务查询、页面、renderer、service/HTTP、新
metadata root/version/owner/ledger/fallback/compat。阶段末集中执行 Web Core contract/preparation/type tests、
AI UI passenger production contract、changed-path type diagnostics/build 与 scoped diff-check，并扫描共享 compiler
中的工具 ID、文案、raw key、shape 和 inferred default 软特调。

实施结果：

- Web Core 在既有 `AiClientToolAnalyticalCapability` 中新增可选 `semanticIntentBindings`，每项 canonical wire 为
  `{ intent, criterion, measures, dimensions }`；版本仍为 `analytical-capability/v1`，session 中仍只有一个
  `expands["x-ai-routing"]`。authoring handle 只接受显式 relationship，compiler 使用与 routing 发布相同的 normalized
  `description.intents` 验证 exact membership，再由唯一 capability normalizer 验证 criterion、measure 与 dimension subset。
- 空 binding/坐标、重复 intent/坐标、未知或跨 declaration intent、criterion/measure/dimension capability drift 均使该
  analytical capability fail closed；合法 sibling 不受影响。未声明 relationship 的 legacy analytical descriptor 不出现新字段，
  因而不获得 direct-admission authority。
- `flow_passenger_trend` 按工具创建时的同一 production i18n intents 显式绑定 `trend`、四个同为
  `crossing-event` unit 的 passenger business measures 与 `time`；`source_record_count` 继续保留在 capability、output binding
  与 Evidence 中作为 completeness/诊断度量，但不进入该自然 intent 的 requested measures；
  device online-rate/message trend 分别绑定自身已发布 intents、唯一 measure 与 `time`。
- `flow_passenger_summary` 在工具创建时从同一 production i18n declaration 取得 exact intents，显式绑定
  `summary`、四个 `crossing-event` 业务 measure 与 `direction`；`area/point`、absolute temporal range 和
  `source_record_count` 仍分别由 scope/temporal/completeness owner 提供。rank/distribution 等 sibling 及普通
  device catalog tools 不发布该字段；未修改 locale 文案、service/query、页面或后端。

冻结 wire 示例：

```json
{
  "version": "analytical-capability/v1",
  "semanticIntentBindings": [{
    "intent": "<exact description.intents member>",
    "criterion": "summary",
    "measures": ["passenger_in", "passenger_out", "passenger_total", "passenger_net"],
    "dimensions": ["direction"],
    "scopeSelection": {
      "binding": "scope",
      "modes": ["project", "explicit"]
    }
  }]
}
```

阶段门禁：Web Core client-tool contract 109/109、preparation 6/6，AI UI passenger production contract 30/30；三者均通过，
其中 passenger 测试从真实 module entrypoint 经 extension registry/runtime/tool-list/session serializer 证明只有
`flow_passenger_summary` 与 `flow_passenger_trend` 发布 typed binding，且 trend 的已冻结坐标未变。Web Core / AI UI /
Device UI production build 分别转换
22,486 / 9,003 / 9,465 个模块并通过。三侧 typecheck 分别复现既有 150 / 699 / 1 条基线诊断，本节路径为 0；首次 AI UI
诊断发现 tuple cast 的 1 条机械错误，修正后由 700 收敛回 699。Device 现有 agent runner 仍为已记录的 21/22 stale oracle，
唯一失败是 property aggregate longitude/latitude，与本节无关；真实 device definition → runtime/session 的 semantic binding
断言已增量写入 `ui/tests/unit/clientToolCatalogCoverage.spec.ts`，但该 production-composed runner 仍受既有 Node strip-types/
federation 装置阻断，因此不把未执行断言或 production build 冒充 runtime assertion 通过。后端 consumer 仍需只从同一 immutable
ToolSurface 的 typed binding 建立 Task intent admission，不得退回 fuzzy matching 或 exact-call relay。
本次 scalar-summary 增量边界复验 Web Core client-tool contract 109/109 与 AI UI passenger production
contract 30/30；AI UI typecheck 仅复现既有 699 条基线诊断，本次 production/test paths 为 0，production
build 转换 9,003 个模块并通过，scoped diff-check 通过。

本次 `scopeSelection` 增量边界复验 Web Core client-tool contract 110/110、preparation 6/6 与 AI UI passenger
production contract 30/30；Web Core / AI UI changed-path type diagnostics 均为 0，AI UI 与 Web Core production build
分别转换 9,003 / 22,486 个模块并通过。真实 passenger summary definition 经 compiler、extension registry/runtime、
tool-list/session serializer 发布上方 frozen 子契约；无 scope binding sibling 不发布该字段。匿名矩阵覆盖 missing/duplicate/
foreign/unknown/reversed mode、scope schema drift 与 cross-declaration intent，均局部 fail closed。通用 compiler/normalizer 未按
passenger/device/bigscreen、工具 ID、description/locale token、用户关键词、raw key/field、shape/mediaType、input order、fixture、
prompt/model 或 inferred default 分支；shape/mediaType 仍只参与显式 canonical port compatibility。两个 owning repo 的全量 dirty
diff-check 通过，protected `generalAgentRealisticBridge.mts` 未被修改。

源码身份：UI / Web Core / AI UI / Device UI HEAD 分别为
`fc50f8fd48b6c246efbafc3a9e4e2f4258bdd4bd` / `add32d128c9281577d454f39bacd67962c240843` /
`69f6ec9aefd98c47101b5a361d3b6f4d8a0ded4f` / `67fdbe9f2269021f8dc03e1b599b34b6cf1a39d1`；Web Core
code/test、AI UI production/test、Device UI production、根 device production test 的 scoped dirty diff SHA-256 分别为
`6be7132a6b583b3273dc381b2e04aef001ff456ba2fef628770d8e7ce16015d4`、
`4d10b5242cf8a2df24d70930fcbfab3b5c7a33d23cd0648525673c8018eb3867`、
`3b87ffa800bdbc656e0002052a2c48cac4c9f9cc1f679179197da48bca21596e`、
`993ce0df0735f001ab4c87a3c02eb1763ae26b852911a346b4e94a83d4de39c2`。这些 hash 覆盖目标文件中保留的既有共享 dirty
hunks，不表示把它们归属或重写为本节改动；protected `generalAgentRealisticBridge.mts` 当前文件 SHA-256 为
`56a38fe6f62ef4fe80c680b3cb0c4b956a943b3bb6cc6f5f9b701d909ee6bcac`，本节未触碰。

### 8.4.6 analytical field type/display-format compatibility 实施计划

目标是让 analytical compiler 仅为物理类型与展示格式相容的 canonical field 发布分析权限，避免
`number + integer` 这类声明漂移进入后端 StructuredBinding。兼容矩阵只由 Web Core 现有 analytical compiler
持有，并同时应用于 static、execution-authored 和 bounded 的编译/执行边界；未声明 `format` 继续允许由
后端默认格式决定。通用 `normalizeAiClientToolOutputFields` 不改语义，legacy/非 analytical 字段不被强制转换；
非法 producer 只局部失去 analytical authority，不影响合法 sibling。

业务侧仅将 `device_query_message_trend` 实际返回的整数消息量字段从 `number` 机械修正为
`integer`，保持 measure/unit/aggregation、HTTP/query、回调、图表和页面不变。非目标包括字符串数字转换、
`ratio + scale=100` 推断、根据工具名/字段名/样本值特调、修改后端或同步 `runtime-ui`。阶段末集中运行
Web Core client-tool contract、Device 工具合同与 scoped `git diff --check`，不逐操作构建。

实施结果：Web Core 在现有 analytical compiler 内新增唯一 `type + displayFormat` 兼容判定，并由
standard static、execution-authored 与 bounded compile/runtime 共用。`normalizeAiClientToolOutputFields` 保持原样，
legacy `string + integer` 仍可归一化，只是不能由格式越权取得 analytical authority。
`device_query_message_trend.value` 已按真实整数回调修正为 `integer + integer`；`runtime-ui` 未修改。

阶段门禁：Web Core `pnpm test:client-tools` 119/119 通过，覆盖 integer/number/decimal/percent 矩阵、
execution-authored 失权、bounded 编译/执行失败关闭与 sibling isolation；三个 owning repo 的 scoped
`git diff --check` 通过。Device `pnpm test:agent-tools` 在测试注册前被现有 esbuild apparatus 阻断：
`@theme-config` 无法解析且 `.vue` 未配置 loader；未为追绿修改 runner/产品代码，也不将未执行的
Device 断言冒充通过证据。

### 8.4.7 producer-localized display unit

目标是让 analytical producer 可在 canonical `unit` 之外声明可选 `unitLabel`，由通用 presentation 链用于
坐标轴、系列和 tooltip 的用户可见单位。`unit` 仍是 measure compatibility、Evidence identity 与 claim grounding
的唯一单位；`unitLabel` 只影响展示，不参与事实匹配，也不能反向授予 analytical authority。

影响范围限于 Web Core 的 `AiClientToolOutputField`、现有 producer 字段声明，以及后端既有
`ProducedOutputBinding -> PresentationSource -> StructuredSourceDescriptor -> PresentationCompiler` 投影链。
前端 renderer、工具参数/schema、Task/Intent、Evidence/Delivery、claim matcher、终答 prose 与 `runtime-ui` 均不修改。
禁止按工具名、业务词、字段名或样本值推断单位；producer 未声明时继续使用 `%` 等既有标准映射，最后回退 canonical
raw unit，不新增兼容双轨或第二 metadata root。

实施按一个冻结合同分两段串行完成：先由 Web Core 唯一类型 owner 增加可选字段，并在 Passenger/Device 的真实 producer
从已有业务 i18n 或物模型 `unitName/unitText/unitLabel` 显式赋值；再由后端唯一 writer 贯通同名字段并只调整
`DisplayProjection.displayUnit()` 的展示优先级。共享 dirty 文件必须基于当前内容做最小 hunk，不覆盖既有 canonical
field、时间编码和 chart compiler 改动。公共 record/DTO 只增加直观字段搬运；不新增业务分支，TraceHolder 与 MBean 不适用。

阶段验证只在两段集成后集中执行：Web Core client-tool contract 与真实 Passenger/Device producer contract，后端
binding/materializer/compiler focused tests，以及一条现有真实 Qwen Passenger 或设备图表场景。不会修改评分器、旧 corpus
oracle 或 browser；失败先按生产输出分类，不为追绿弱化合同。

实施结果保持单一 wire 字段 `unitLabel`：前端通用归一化、Passenger 客流字段、Device 物模型单位和后端上述
六段投影链均已贯通。展示优先级为 `unitLabel` -> canonical 标准映射 -> raw `unit`；Unicode 本地化单位使用
精确子串去重，ASCII 单位保留词边界。该字段未进入 authority、semantic equality、Evidence identity、claim grounding
或工具参数，没有增加工具名、业务词、字段名或样本值分支。

集中验证结果：后端 binding/materializer/compiler 71/71，Passenger producer 36/36，Device producer 28/28，
Web Core 新增 `unitLabel` 合同用例通过，scoped `git diff --check` 通过。Web Core 全量命令仅保留一个既有
Device tools 内部类型导入架构守卫失败，不为本合同追绿。真实 Qwen 3.7 Passenger 趋势调用产出了自然趋势
结论、一个 typed ECharts block、完整 Evidence/Delivery 且 grounding 保持 `GROUNDED_KEEP`；评测命令的非零退出
来自旧 oracle 将“分析准备 + 业务查询”的两个合法 logical call 按全局 exactly-once 计数，以及将运行时填充
时间参数计为 argument-scope 失配；本阶段未修改 evaluator/oracle。真实 bridge fixture 未携带本地化字段，
因此 `unitLabel` 端到端展示证据以真实 producer contract 和后端 materializer/compiler 测试为准，不将该
Qwen 样本冒充为单位 wire 证据。

### 8.4.8 同轮多个 typed presentation

目标是让同一轮中多个已验证、已完成且各自拥有精确 analytical source lineage 的结构化结果，分别物化为多个
独立 presentation resource，并由既有 canonical composer 按提交顺序交付。该行为对齐 Codex 的 typed content
数组语义：每个非文本结果保持独立 block，不把不同来源、量纲或坐标轴猜测合并成一个图。

影响范围仅限后端 `TurnPresentationObligation` 对 server-owned analytical default source 的授权投影。
当前单例 default source 在第二个合法趋势注册时进入全局 conflict，导致第二个 source 在 materializer 之前丢失；
下游 materializer 的“每个调用最多一个 presentation”、DeliveryProof、`requiredResourceIds` 聚合和 composer 多 block
顺序语义均保持不变。UI、producer/tool schema、PresentationCompiler、TerminalResponsePipeline、评测器和
`runtime-ui` 不修改。

实施将单例 default source 改为有界、插入有序、按完整 `AnalyticalSource` 去重的集合；每次 invocation 只允许与
当前 definition、port 和 Evidence lineage 精确匹配的成员投影。授权刷新逐成员撤销，仍须保留 selected analytical
authority 下 structured lineage 不得自授权、binding-key collision fail closed、stale/forged/sibling source 不得
物化等现有边界。禁止按工具名、业务词、字段名、单位或调用先后猜测合并。

每个独立 analytical source 还必须拥有 source-local delivery requirement identity。该 identity 只由已验证
`AnalyticalSource` 的 task epoch、requirement revision/digest、scope digest、capability id/digest 和 output port
组成；不得使用 payload digest、Evidence id、工具名、用户文本或 renderer 猜测。同一逻辑来源重试保持相同
deliverable key 并只 supersede 自己的旧 revision，不同来源即使 renderer/type 相同也必须得到不同 key。
显式 Task/delivery slot 继续优先；generic capability slot 只保留给非 analytical legacy 输入。

阶段验证集中覆盖：两个不同 TREND source 各自产生一个精确 projection/resource，重复注册去重，撤销/伪造/
sibling source fail closed，单趋势行为不变；随后直接复跑现有真实 Qwen 设备多意图场景，验收两个独立 ECharts
block、不同 deliverable key、各自 producerCallId/Evidence digest/Delivery lineage、两个 committed/composed proof，
以及 prose + chart A + chart B 的稳定顺序。

实施与真实验证已完成：`TurnPresentationObligation` 保留有界、可撤销的 exact analytical source 集合，
`ToolOutcomeProcessor` 按 Task/显式交付 -> exact analytical source -> generic renderer fallback 选择 slot，不再让
通用 ECharts pending 把两个独立来源归入同一 revision chain。真实 Qwen 3.7 设备多意图运行产出自然模型
prose、两个按顺序的 ECharts block；两个 runtime resource 均为 `materialized`，且 deliverable key、
producerCallId、Evidence digest、proof 与 lineage 均独立，terminal narrative 保持 `GROUNDED_KEEP`。命令的非零退出
仍来自旧 `callbackExactlyOnce` / `argumentScopeExact` oracle；本阶段未修改 evaluator。观察器中
`deliverableCommitted=1` 与 `deliveryComposed=1` 是整轮状态转换和 canonical composition 事件数，不是
resource cardinality；多 block 数量继续由 `presentationCount`、runtime resources 与最终 content blocks 验收，不为追数量修改评测器。

### 8.5 工具结果生命周期与展示状态

`session.tool_result` 是工具调用的终态事件；结果中的 `complete/truncated/completeness` 只描述本次结果交付完整性，
`supportsAbsenceClaim` 单独描述空结果结论权限，三者不得互相反推。Web Core 的 `clientToolResult.ts` 是通用结果状态解析的唯一
owner，按 canonical `evidence`、顶层 canonical mirror、已发布 legacy `success/ok/status/outcome` 的顺序生成只读投影：
`executionStatus`、`resultCompleteness`、`evidenceCoverage`、`absenceAuthority` 和 conflict diagnostic。canonical 与 mirror 冲突、
malformed completeness 或 partial source 声称 absence authority 时必须 fail closed，不能提升为 complete/supported；缺少 completeness 的
legacy success 仍可完成调用，但展示为 unknown，不能猜成完整。

Agent Conversation 只消费该共享投影：收到 `session.tool_result` 后按 `executionStatus` 将对应 tool call 收敛为 completed/failed，
不再把 partial 当作运行中状态；结果卡把 complete 显示为“调用完成”，把 partial/truncated 显示为中性告警“调用完成 · 范围有限”，
unknown 只显示调用已结束，失败才使用红色。前端不得据此改写 assistant transcript、生成终态 fallback、解析业务字段或推断不存在结论。
现有业务 producer、HTTP/query、`x-ai-routing`、64 KiB guard、canonical presentation 和后端 Terminal Decision 均不在本切片修改范围。

验证使用匿名 canonical complete、partial-empty、partial-non-empty、authoritative empty、legacy success、failure、malformed/conflict sibling；
同时断言 partial result 的 tool-call 生命周期已经结束、结果卡不显示普通绿色成功、complete empty 与 absence authority 仍保持独立。
阶段边界集中执行 Web Core client-tool contract/type tests、Agent Conversation state tests、聚焦 typecheck、production build 与 diff check。
2026-08-20 最终源码新增 Agent Conversation 9/9 状态测试并通过；Agent UI 独立 production build 转换 11,267 个模块，AI UI
独立 build 转换 9,002 个模块，全量 Web Core build 转换 22,485 个模块，三个构建均通过。Agent UI 模块级 `vue-tsc` 仍复现
313 条既有跨模块基线诊断；本切片新增生产文件为 0 诊断，`index.vue` 仅保留本切片前已有的确认态与计划态两处诊断，未新增回归。

## 9. 非目标与门禁

禁止：

- 根据工具名、页面、设备、属性名、字段名、模型/provider 或用户句式特调；
- 在浏览器从 records/aggregate 派生 ECharts option；
- 自动把 content 视为安全 inline；
- 缺少 audience 时根据工具名、payload shape、recordPath 或运行时 carrier 猜 projection；
- 用 `client-presentation` 或 `reusable-source` 的完整内容补偿 `model-evidence` grounding 缺口；
- 让模型构造文件路径、签名 URL、完整 source 或物理 binding；
- 把 preview、model sample 或 viewport 数量冒充完整结果；
- 复制实现到 runtime-ui。

验证门禁：

- clientToolContract.test.ts 与 clientToolDefinition.type-test.ts；
- 大结果、空结果、安全 inline、超限 preview、取消、上传失败和幂等补偿测试；
- binding identity、range、ordering、数量与 partial 语义回归；
- renderer 测试证明前端只消费 canonical source；
- iot-ui 等 owning module 的窄构建；
- git diff --check 与旧 presentation adapter/场景特调扫描。

历史交付：统一大结果与补偿边界已由 PR #99 合并；浏览器 presentation 写路径由 PR #98 删除。具体测试数量、覆盖率和构建日志以 PR/CI 为准，不在本规范重复保存。
