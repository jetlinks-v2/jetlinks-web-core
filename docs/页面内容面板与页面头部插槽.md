# 页面内容面板与页面头部插槽

## 1. 目标与当前结论

**项目布局的布局壳层统一给每个路由页面套 `ContentPanel`，默认开启，页面代码不需要自己包裹。**

三条规则：

1. **默认接管，但只在项目布局**：`CONTENT_PANEL_LAYOUT_VARIANTS = ['project']`，只有项目布局（`ProjectLayoutPage` → `BasicLayoutShell`）通过 `RouteContentSurface` 给路由内容套面板，此时 `DEFAULT_CONTENT_PANEL_ENABLED = true`。租户端复用同一个 `BasicLayoutShell`，但保持引入面板前的内容区结构；应用端 `ApplicationLayoutPage` 不经过 `RouteContentSurface`，是否包裹面板由该壳层自己决定。
2. **不做页面内容判断**：壳层**不去猜**页面里有没有 `ContentPanel`。壳层是跨路由常驻的，任何"按页面内容推断"的状态都会被带到下一个页面上去（见第 3.3 节的踩坑记录）。
3. **例外走路由 meta**：不需要面板、或只需要布局不要背景与阴影的页面，用 `meta.contentPanel` 声明（只在项目布局解析）。

页面里原有的 `ContentPanel` 由各模块**逐个删除替换**：删掉后页面自动获得布局面板，不会再出现双层卡片。

## 2. 影响范围与 owning module

- **owning**：`runtime-ui/jetlinks-web-core`
  - `src/router/types.ts`（meta 类型）
  - `src/layout/hooks/useRouteContentPanel.ts`（meta 解析 + 默认开关）
  - `src/layout/hooks/usePageChromeTarget.ts`（面板外头部容器）
  - `src/layout/components/RouteContentSurface/`（壳层内容区）
  - `src/components/ContentPanel/`、`src/components/PageChrome/`
  - `src/layout/shells/BasicLayoutShell.vue`、`src/layout/shells/BasicLayoutShell.less`、`src/layout/shells/ApplicationLayoutPage.vue`
- **消费方**：`runtime-ui/modules/*-ui` 的路由页面（约 200 个 `.vue`）
  - 没写 `ContentPanel` 的页面：**零改动**，在项目布局下直接获得统一面板
  - 写了 `ContentPanel` 的页面（约 98 个文件）：需要**逐页删除页内面板**，过渡期会短暂出现双层卡片
- **生效范围**：**只有项目布局**。租户端复用 `BasicLayoutShell`，但不挂 `--panel` 修饰类、不包裹面板；应用端 `ApplicationLayoutPage` 不经过 `RouteContentSurface`，保持自身结构
- **不涉及**：`ui/`（运营端）；外部 npm 包 `@jetlinks-web/components` 的 `j-page-container` 不动

现状统计（实测）：`j-page-container` 198 个文件、`FullPage` 144 个、`ContentPanel` 98 个，其中 `FullPage` + `ContentPanel` 同文件 64 个。

## 3. 契约

### 3.1 路由 meta

```ts
// vue-router RouteMeta 增强
contentPanel?: boolean | {
  enabled?: boolean    // false = 壳层不包裹面板，页面自绘背景
  padding?: number     // 覆盖 ContentPanel 内边距
  title?: string       // 面板标题
  background?: boolean // false = 保留面板留白/圆角，但背景透明、去模糊、去阴影
}
```

- 取值规则与 `RouteMeta.layoutClassName` 一致：沿 `route.matched` 由深到浅取第一条声明了 `contentPanel` 的记录。
- `meta.contentPanel: false` 等价于 `{ enabled: false }`。
- 三条兜底路径（未声明 / 布尔真 / 对象未写 `enabled` / 值类型非法）全部落到 `DEFAULT_CONTENT_PANEL_ENABLED = true`。
- **只在项目布局解析**：非项目布局在 `useRouteContentPanel()` 处直接返回 `{ enabled: false }`，连 `meta.contentPanel: true` 也不会生效，保证面板不会逸出项目布局。
- 典型用法：
  - 全屏画布 / iframe / 需要自绘背景 → `contentPanel: false`
  - 只要布局对齐、不要白底和阴影 → `contentPanel: { background: false }`
  - 需要统一面板但内边距不同 → `contentPanel: { padding: 0 }`
- 动态菜单路由的 meta 会合并菜单项的 `options.meta`（`src/utils/menu.ts` 的 `handleMeta`），该 meta 既可写在模块路由里，也可写在菜单配置里。

### 3.2 壳层内容区结构

```
<section class="project-layout__route-content [--panel]">
  <div class="route-content-surface route-content-surface--panel">
    <div ref="chromeElement" class="route-content-surface__chrome"></div>   <!-- 面板外头部容器 -->
    <ContentPanel fill>                                                      <!-- 统一面板 -->
      <PageRouteView />
    </ContentPanel>
  </div>
</section>
```

`meta.contentPanel` 关闭时退化为改造前的结构：`<div class="route-content-surface__chrome">` + `<PageRouteView />` 两个同级节点（空容器被 `:empty { display: none }` 隐藏）。

### 3.3 为什么不判断"页面里有没有 ContentPanel"（已踩过的坑）

第一版实现让页面里的 `ContentPanel` 上报"我自己有面板"，壳层面板据此让位（`display: contents`）。
`suppressed` 是壳层 setup 里创建的 `ref`，**壳层跨路由常驻**，所以：

> 从一个自带 `ContentPanel` 的页面跳到没有 `ContentPanel` 的页面后，`suppressed` 仍然是 `true`，
> 壳层面板一直停在 `content-panel--yielded` 上 —— 新页面拿不到背景和阴影。

DevTools 上能看到 `section.content-panel.content-panel--yielded.route-content-surface__panel`。

结论：**面板是否包裹只允许由路由 `meta.contentPanel` 决定**，这是一个随路由变化、无残留的状态。
任何"按当前页面内容推断"的壳层级状态都是跨路由泄漏源，不要再引入。

### 3.4 面板外页面头部

只有"头部必须落在页面板之外"的页面需要改代码：把那段内容放进 `PageChrome`。

```vue
<template>
  <!-- 页签/卡片切换充当面板顶栏：flush 让它与面板贴合 -->
  <PageChrome flush>
    <SlantedTabs :activeKey="activeType" :options="tabs" @change="changeType" />
  </PageChrome>

  <!-- 页内 Panel 删掉，内容直接写；面板由布局提供 -->
  <FullPage flex transparent-background>
    <EqualHeightColumns>...</EqualHeightColumns>
  </FullPage>
</template>
```

| 头部形态 | 写法 | 效果 |
| --- | --- | --- |
| 页签/卡片切换充当**面板顶栏** | `<PageChrome flush>` | 与面板连成一体（壳层闭合间距 + 面板左上角拉平） |
| 返回按钮、独立操作条 | `<PageChrome>` | 保留壳层默认间距；自身可再加 `margin-bottom` |

`flush` 用 `:has(> .route-content-surface__chrome > .page-chrome--flush)` 纯 CSS 判定，
不引入跨路由组件状态——这正是第 3.3 节踩过的坑要避免的东西。

- `PageChrome` 基于 `Teleport`，**保留页面自身的 provide/inject 与 scoped 样式**。
- 目标元素由 `RouteContentSurface` 通过 `provide` 下发元素引用（不是选择器：挂载期祖先还没连到
  `document`，`querySelector` 查不到）。页面在 `onMounted` 读取——壳层的模板 ref 走 post-render 队列。
- 拿不到目标时（BlankLayout / 全屏页 / 微应用宿主）`Teleport` 保持 `disabled`，内容原地渲染，不丢内容。
- 参考实现：
  - 贴合式 `modules/device-manager-ui/views/device/list/unified/index.vue`
  - 独立式 `modules/device-manager-ui/views/device/list/components/IotDeviceDetailView.vue`

### 3.5 fill 面板里的页面伸缩契约

面板接管时，页面根节点会成为 flex 项：

| 页面形态 | 页面根节点写法 |
| --- | --- |
| 自己算高度 / 用 `FullPage` 的整屏页面 | 保持不动（默认 `flex: 0 1 auto`，会收缩到面板高度） |
| 按内容高度排布、由面板滚动的页面 | `flex: 0 0 auto` |

不要依赖百分比高度：壳层链路是 flex 定高，百分比不可靠。

### 3.6 生效范围：只有项目布局

`RouteContentSurface` 被 `BasicLayoutShell` 共用，而 `BasicLayoutShell` 同时服务项目端（`ProjectLayoutPage`）
与租户端（`TenantLayoutPage`）。所以"是否包裹面板"不能只看路由 meta，还要先看布局变体：

```ts
export const CONTENT_PANEL_LAYOUT_VARIANTS: readonly BasicLayoutVariant[] = ['project']

export const useRouteContentPanel = () => {
  const route = useRoute()
  const variant = useBasicLayoutVariant()

  return computed<ResolvedRouteContentPanel>(() => {
    // 非项目布局：不解析 meta，直接退化为改造前的内容区结构
    if (!isContentPanelLayout(variant.value)) {
      return { enabled: false, background: true }
    }
    return resolveRouteContentPanel(route.matched)
  })
}
```

判定放在 hook 里而不是壳层里，是因为 `BasicLayoutShell`（决定 `--panel` 修饰类）与
`RouteContentSurface`（决定是否包裹）必须拿到同一个值；**壳层选择本身就由 `useBasicLayoutVariant()` 决定**
（`BasicLayoutPage.vue`），所以两者天然一致，不需要额外传参。

非项目布局下：

- `BasicLayoutShell` 不加 `.project-layout__content--panel` / `.project-layout__route-content--panel`，
  内容区回到改造前的块级布局；
- `RouteContentSurface` 走 `chrome 容器 + PageRouteView` 分支（空容器 `:empty { display: none }`）；
- `meta.contentPanel` 完全不参与解析，包括显式 `true`。

应用端 `ApplicationLayoutPage` 不经过 `RouteContentSurface`，它的面板包裹由该壳层自己保留，不受本节影响。

## 4. 关键代码落点

| 文件 | 职责 |
| --- | --- |
| `src/router/types.ts` | `RouteContentPanelMeta` 与 `RouteMeta.contentPanel` 类型声明 |
| `src/layout/hooks/useRouteContentPanel.ts` | 解析 meta；持有 `CONTENT_PANEL_LAYOUT_VARIANTS`（当前仅 `project`，非项目布局直接返回不包裹）与 `DEFAULT_CONTENT_PANEL_ENABLED`（当前 `true`） |
| `src/layout/hooks/usePageChromeTarget.ts` | 面板外头部容器的 provide / inject |
| `src/layout/components/RouteContentSurface/index.vue` | 壳层内容区：面板 + 头部容器 + `PageRouteView`；两个壳层共用 |
| `src/components/ContentPanel/index.vue` | `background` / `fill` prop；fill 模式转 flex 列并跳过空标题占位 |
| `src/components/PageChrome/index.vue` | 面板外头部插槽组件，Teleport + 原地兜底；`flush` 与面板贴合 |
| `src/layout/shells/BasicLayoutShell.less` | `.project-layout__content--panel` / `.project-layout__route-content--panel` |

两个壳层（`BasicLayoutShell.vue`、`ApplicationLayoutPage.vue`）原本各自复刻
`<div class="project-layout__route-content"><PageRouteView /></div>`，本次统一收敛到
`RouteContentSurface`，避免第三处重复。

## 5. 菜单驱动路由的 meta 不随代码生效

模块 `baseMenu.json` 只在初始化流程（`views/init-home` → `updateMenus`）推送到后端；
运行时菜单来自 `POST /menu/user-own/tree`。所以已初始化的环境改 `baseMenu.json` **不会自动生效**，
需要在菜单管理里同步 `options.meta.contentPanel`，或重新执行菜单初始化。

本地临时验证例外效果，可直接把 `useRouteContentPanel.ts` 的 `DEFAULT_CONTENT_PANEL_ENABLED` 临时改成 `false`。
需要验证"非项目布局不包裹面板"时，把运行作用域切到租户端（`VITE_APP_RUNTIME_SCOPE=tenant`），
或临时把 `'tenant'` 加进 `CONTENT_PANEL_LAYOUT_VARIANTS` 再改回来。

## 6. 页面迁移清单与进度

### 6.1 迁移步骤

1. 删掉页面最外层的 `<ContentPanel>` / `<FullPage><ContentPanel>` 包裹，内容直接写。
2. 如果顶部有返回按钮、卡片切换、页签等**必须留在面板外**的内容，用 `PageChrome` 包起来；
   页签类"面板顶栏"用 `<PageChrome flush>`，返回按钮类用 `<PageChrome>`。
3. 整屏页面保留自己的高度机制（`FullPage` / 自算高度），不要改成依赖面板定高；
   作为布局面板的 flex 项时加 `flex: 0 1 auto; min-height: 0`，让它收缩到面板内容高度，
   页内滚动仍交给页面自己的滚动容器，避免第二条滚动条。
4. 页面确实需要"不要背景 / 不要阴影 / 不同内边距"时，用路由 `meta.contentPanel`，不要靠页内面板实现。

### 6.2 第一批：删除页面级包裹（已完成）

范围：`modules/*/views/**`，**不含** `views/**/components/**`（22 个文件）与 `modules/*/components/**`（3 个文件）。

| 项 | 数量 |
| --- | --- |
| 页面文件里 `<ContentPanel>` 总出现次数 | 67（分布在 64 个文件） |
| 本次删除的**页面级包裹** | **55**（11 个模块） |
| 保留的**内层子面板** | 12 |

删除规则：

- 裸标签（49 处）→ 直接删 `<ContentPanel>` 与 `</ContentPanel>`；
- 带 `class` 的（6 处）→ 换成同属性 `<div>`，**class 原样保留**。
  这些 class 挂着页面自己的布局（`flex: 1` / `height: 100%` / `display: flex` / `overflow: hidden`），
  直接删元素会让页面塌陷，和"整页空白"是同一类失败：
  `device-batch-page`、`plugin-distribution-page`、`model-list__content`、`product-page__main`、
  `menu-detail-container`、`template-summary-content`。

按模块分布：authentication-manager-ui 17、jetlinks-media-ui 9、device-manager-ui 7、
jetlinks-ai-ui 6、rule-engine-manager-ui 5、data-collector-ui 4、edge-master-ui 2、
notify-manager-ui 2、datasource-manager-ui / jetlinks-function-ui / jetlinks-space-ui 各 1。

### 6.3 保留的 12 处内层子面板（不是页面外框）

它们要么在 `EqualHeightColumns` 的 `<template #left/#right>` 插槽里（分栏面板），
要么是带 `data-html2canvas-ignore` 的导出工具栏，删了会丢子区域卡片 / 影响导出截图：

| 文件 | class |
| --- | --- |
| `edge-master-ui/.../gateway/GatewayListPage.vue` | `gateway-access-table` |
| `jetlinks-ai-agent-ui/.../LargeModelManagement/index.vue` | `llm-provider-ledger` |
| `jetlinks-ai-agent-ui/.../Skills/index.vue` | `skills-list-ledger` |
| `jetlinks-ai-agent-ui/.../Tools/index.vue` | `tools-ledger` |
| `jetlinks-ai-agent-ui/.../applicationList/index.vue` | `agent-list-ledger` |
| `jetlinks-ai-ui/.../points/Board/index.vue` | `flow-point-board__aside`、`flow-point-board__main` |
| `jetlinks-ai-ui/.../portrait/index.vue` | `flow-portrait-toolbar`（html2canvas） |
| `jetlinks-ai-ui/.../trends/index.vue` | `flow-trend-toolbar__main`（html2canvas）、`flow-trend-card` |
| `jetlinks-ai-ui/.../machine-vision/VisualScene/index.vue` | `scene-panel` |
| `saas-runtime-ui/.../PersonCenter/index.vue` | `content-card` |

### 6.4 待跟进：头部仍在面板内

以下几个页面的顶部元素原本在自己 `ContentPanel` 外面，删掉页内面板后它们落进了布局面板里，
要让它们回到"面板外"需要改用 `PageChrome`：

- `edge-master-ui/views/workbench/gateway/GatewayDetailPage.vue` 的返回按钮（`gateway-detail__back`）
- `authentication-manager-ui/views/system/Platforms/Api/index.vue` 的标题区
- `jetlinks-ai-ui/views/flow-analysis/portrait/index.vue` 的 `PageHeader` 与工具栏

### 6.5 迁移后验证

- 55 个改动文件全部通过 `@vue/compiler-sfc` 编译（标签配对若错会直接编译失败）。
- 逐文件断言：55 个目标文件里 `<ContentPanel>` / `</ContentPanel>` 出现次数均为 0
  （`portrait/index.vue` 保留 1 对，即上面那个 html2canvas 工具栏）。
- 统计所有被删除的开标签：49 个裸标签 + 6 个仅带 `class`，**没有丢失任何其它属性**。
- 未执行：浏览器逐页复核（11 个模块、55 个页面），以及删除后各页面高度/滚动条是否正常。

### 6.6 声明不包裹面板的页面（初始化菜单 meta）

以下 7 个整屏/看板类页面在**模块 baseMenu.json 的菜单项上**声明 `meta.contentPanel = false`，
布局壳层不再给它们套面板（页面自绘背景）：

| 菜单路径 | code | 文件 |
| --- | --- | --- |
| 概览 | `project/Overview` | `authentication-manager-ui/baseMenu.json` |
| 资源中心 > 仪表盘 | `resources/Dashboard` | `authentication-manager-ui/baseMenu.json` |
| 资源中心 > 空间管理 > 空间态势 | `space/Situation` | `jetlinks-space-ui/baseMenu.json` |
| 资源中心 > 空间管理 > 空间驾驶舱 | `space/WK-Situation` | `jetlinks-space-ui/baseMenu.json` |
| 运维中心 > 仪表盘 | `link/DashBoard` | `device-manager-ui/baseMenu.json` |
| 告警中心 > 事件记录 > 视觉事件 | `video/event` | `jetlinks-ai-ui/baseMenu.json` |
| 告警中心 > 告警规则 > 视觉告警 | `machine-vision/VisualAlarm` | `jetlinks-ai-ui/baseMenu.json` |

写法：**菜单项的 `meta` 与 `options.meta` 两处都写** `contentPanel: false`。

- `project/Overview`、`resources/Dashboard` 合并进已有的 `meta` / `options.meta` 对象；
- `space/Situation`、`space/WK-Situation`、`link/DashBoard`、`video/event`、`machine-vision/VisualAlarm`
  按需新增 `meta`、`options`（`video/event` 原本是空的 `options: {}`，改成只含 `meta` 的对象）。

两处都写是因为解析时两者都会参与合并（`src/utils/menu.ts` 的 `handleMeta`：
`{ ...item.options?.meta, ...item.meta }`，同名时 `item.meta` 胜出），
菜单配置侧读取的是 `options.meta`，两处保持一致可以避免后续维护时只改一处。

注意两点：

1. 生效链路是 `baseMenu.json` → `handleMenu`（`...item` 原样保留 `meta` 与 `options`）→ 初始化流程
   `views/init-home` 的 `updateMenus` → 后端 `/menu/{owner}/_all` → 运行时 `POST /menu/user-own/tree`
   生成路由 meta。**已初始化的环境必须重新同步菜单（或执行菜单初始化）才会生效**，改文件本身不会热生效；
   且这些声明**只在项目布局生效**（租户端/应用端不解析 `meta.contentPanel`）。
2. `空间管理 > 空间驾驶舱`（code 同为 `space/WK-Situation`，url `/space/WK-Situation`）是另一个菜单项，
   本次**没有**改动；`资源中心 > 可视化 > 仪表盘`（`view/dashboard`）同样不在本次范围内。

过渡期（页内面板还没删）会看到双层卡片，这是预期的临时状态。

## 7. 明确不做

- 不改 `ui/` 运营端
- 不改外部包 `j-page-container`
- 不在壳层里按页面内容推断是否包裹面板
- 不引入新的全局状态库，不重写 `PageRouteView`
- 不在本次调整页面的整屏高度机制（`FullPage` / 自算高度）

## 8. 风险与待确认点

| 风险 | 说明 | 应对 |
| --- | --- | --- |
| **壳层级状态跨路由泄漏（已发生）** | 壳层常驻，任何"按页面内容推断"的状态会带到下一个页面 | 包裹与否只由 `meta.contentPanel` 决定，无残留状态；已移除让位机制 |
| 过渡期双层卡片 | 页内面板还没删时，壳层面板 + 页内面板会叠加 | 按第 6 节逐页删除页内面板；这是计划内的临时状态 |
| `FullPage` 页面轻微双滚动 | 144 个页面用 `FullPage` 自算 `calc(100vh - top - padding)`，套进内部滚动面板后可能差几像素 | `FullPage` 会作为 flex 项收缩到面板高度；幅度小，需浏览器复核 |
| 页内面板 `height: 100%` 失效 | 壳层链路是 flex 定高，百分比高度不可靠 | 迁移时同时去掉页内面板；整屏页面保留自己的高度机制 |
| 菜单 meta 不随代码生效 | 见第 5 节 | 例外页面通过菜单配置或模块路由 meta 声明 |
| `:empty` 依赖 | `.route-content-surface__chrome` 用 `:empty` 隐藏 | 目标节点保持纯空元素（模板里不写 `v-if` / 注释） |
| 租户端面板缺口（本次收窄后暴露） | 本仓库 `.env.development` / `.env.production` 都是 `VITE_APP_RUNTIME_SCOPE=project`，租户端当前不会被构建；一旦启用租户端，第 6.2 节已删除页内面板的 55 个页面会没有面板 | 启用租户端前需要重新评估：或把 `'tenant'` 加进 `CONTENT_PANEL_LAYOUT_VARIANTS`，或按页面补回面板 |

## 9. 验证方式

已执行：

- `vue-tsc --noEmit -p jetlinks-web-core/tsconfig.json`：本次新增/修改文件无新增错误。
  该 tsconfig 本身存在大量历史错误（`src/api/**`、`src/components/**`）与 `@jetlinks-web/vite`
  依赖类型错误，因此只按"本次改动文件是否出现新错误"为判据。复核命令：
  `node --max_old_space_size=8192 node_modules/vue-tsc/bin/vue-tsc.js --noEmit -p jetlinks-web-core/tsconfig.json 2>&1 | grep -E "RouteContentSurface|PageChrome|useRouteContentPanel|usePageChromeTarget|ContentPanel/index|BasicLayoutShell|ApplicationLayoutPage|router/types|components/index"`
- 仅剩 `src/layout/shells/ApplicationLayoutPage.vue(174,84)` 一条 `stripMenuChildren` 类型错误；
  该函数与 HEAD 逐字节一致（`git diff` 已确认本次未触碰），属历史遗留。
- `@vue/compiler-sfc` 编译本次改动的 SFC：全部 `ALL SFC COMPILE OK`。

未执行（环境限制，待补）：

- `pnpm -F jetlinks-web-core build`：本机 DSH 内置的 Node 运行时堆上限受限，
  `--max_old_space_size` 给到 32G 仍在约 3.8G 处 `JavaScript heap out of memory` 退出。
  本地请用 `pnpm build`（脚本自带 `--max_old_space_size=81920`）。
- 浏览器实测（按顺序）：
  1. **回归复现项**：从自带 `ContentPanel` 的页面跳到没有 `ContentPanel` 的页面，
     新页面必须有面板背景与阴影（本次修复的目标）。
  2. 反复来回跳 3 次以上，面板状态不能"粘住"。
  3. 未写 `ContentPanel` 的页面：面板撑满内容区、不双滚动条。
  4. `meta.contentPanel: { background: false }` 与 `false` 各一例。
- 检查项：滚动条数量、面板高度撑满、头部与面板间距、透明面板无白底无阴影

## 10. 交付回填

### 10.1 收窄为「仅项目布局」（本轮）

改动：`useRouteContentPanel.ts` 增加 `CONTENT_PANEL_LAYOUT_VARIANTS = ['project']` 门禁，
非项目布局不解析 `meta.contentPanel`；`BasicLayoutShell.vue` / `BasicLayoutShell.less` 注释同步口径；
`router/types.ts`、`components/ContentPanel/README.md` 与本文档范围说明同步。
`ApplicationLayoutPage` 按决定**保留**自身面板结构，未改动。

已执行：

- `pnpm -F jetlinks-web-core build -- --module-name authentication-manager-ui`：
  `✓ built in 49.26s`，退出码 0（宿主编译覆盖 core 模板与模块页面）。

未执行：

- 本模块没有为这次收窄新增自动化用例，判定入口只有 `CONTENT_PANEL_LAYOUT_VARIANTS = ['project']`
  与 `isContentPanelLayout()` 这一处代码门禁；`application` 变体同理，由
  `isContentPanelLayout('application') === false` 兜住（不依赖用例）。
- 浏览器实测：租户端（`VITE_APP_RUNTIME_SCOPE=tenant`）下页面应无壳层面板、无 `--panel` 修饰类，
  内容区回到改造前的块级布局；项目端行为不变。
- 其余待补项见 10.2。

交付：

- Git commit hash：`jetlinks-web-core@c7e141b`
- Pull Request 地址：https://github.com/jetlinks-v2/jetlinks-web-core/pull/148

### 10.2 路由级内容面板首次交付（历史）

- 验证结果：typecheck（按改动文件范围）与 SFC 编译通过；build 与浏览器实测 pending
- Git commit hash：`jetlinks-web-core@61dfbff`（#147）；模块侧 `authentication-manager-ui@15fb02d`（#77）
- Pull Request 地址：https://github.com/jetlinks-v2/jetlinks-web-core/pull/147
